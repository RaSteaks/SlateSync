# SlateSync Implementation Package IP-0102

STATUS: **COMPLETED — APPROVED BY GATE 01-02 ON 2026-08-21**

DOCUMENT CLASS: **HISTORICAL EXECUTION PACKAGE — NOT RESUMABLE**

BATCH: IP-01 + IP-02

AUTHORIZED BY: Gate 00 `APPROVED`

REVIEW TARGET: `.codex/refactor/reviews/GATE-01-02.md`

BASELINE HEAD: `c7dafa4d972e5eb7be61f00e2b546d6826e70c87`

GENERATED: 2026-08-20

FINAL DISPOSITION: Correction C01 closed B01-B03 and M01-M03. This package is
historical execution evidence and no longer authorizes additional edits. The
only next implementation authority is `IP-03-08-CONTINUOUS.md`.

## Owner Policy Update — 2026-08-20

IP-01 and IP-02 remain a separate executable foundation batch and all non-visual Stop Conditions, acceptance tests, scopes, and Gate-01/02 review requirements below remain binding. After Gate 01-02, the only downstream package is `IP-03-08-CONTINUOUS.md`; references below to entering or authorizing `Batch 03-04` now mean admitting that continuous package.

The Gate-00 visual carryover is transferred to the continuous package's unified post-UI visual baseline. Its missing PNG/manifest evidence no longer blocks non-UI IP-01/02 completion, Gate-01/02 approval, or the start of non-UI continuous architecture work. It remains enabled debt and must pass, without weakened tests or fabricated captures, before the Continuous Completion Report can be ready for Sol Final Review.

## Current Repository Facts

- The repository-root `AGENTS.md` is absent. The active repository instruction supplied to this task remains binding: after editing code, add or update comments for non-obvious architecture, ownership, concurrency, compatibility, and lifecycle behavior, and remove stale comments.
- The application is currently JavaScript-only. There is no `src/**`, TypeScript, Vite, Vitest, React, or modern build configuration.
- Production startup is `electron/main.mjs` → `electron/preload.cjs` → `public/index.html` / `public/app.js`.
- The current Preload exposes `window.electronAPI`; `public/electron-bridge.js` is its only Renderer consumer. There are 27 request methods plus the `recognition-progress` event.
- `package.json` uses Node `>=20.19`; CI and release use Node 22; Electron is `43.3.0`. `out/` is already ignored by Git.
- The working tree already contains the approved Gate-00 artifacts and the Architect's batching-document edits. Luna must record this start inventory and must not clean, overwrite, stage, or commit it.
- On 2026-08-20, `npm run check` passed. `npm test` ran 226 tests: 225 passed and the only failure was the known missing `.codex/refactor/baseline/visual/manifest.json`; no test was skipped or marked todo.
- Signed packaging is not required. The accepted local packaging command is `CSC_IDENTITY_AUTO_DISCOVERY=false npm run electron:build:dir`.

## Batch Execution Contract

1. Luna executes IP-01 first and records an IP-01 local checkpoint before touching IP-02-only scope.
2. IP-01 and IP-02 scopes remain separate. A path allowed by one IP is not automatically allowed by the other.
3. IP-02 may extend the IP-01 `src/preload/**` and `src/shared/**` skeletons and the named package/test configuration because this succession is explicit below. Other IP-01 outputs are provisionally frozen at the local checkpoint.
4. A new failure, scope ambiguity, Architecture Invariant conflict, unlisted dependency, or blocking Decision Queue entry stops the entire batch. Luna must not continue into the next IP merely because the eventual review is joint.
5. Luna runs every IP-specific validation at its local checkpoint and every cumulative validation before handoff.
6. Luna produces one Batch Completion Report containing separate `PACKAGE: IP-01` and `PACKAGE: IP-02` sections, the start inventory, both local diffs, all commands/results, and `NEXT BATCH ENTERED: NO`.
7. Luna must not commit, stage, push, switch branches, clean untracked files, edit this package, or enter IP-03.

## Gate-00 Visual Carryover

The visual baseline remains required final evidence, but under the Owner Policy Update it is no longer a Batch 01-02 deliverable or admission blocker. Batch 01-02 may still resolve it within the scope below; otherwise preserve the enabled failure and transfer it explicitly to `IP-03-08-CONTINUOUS.md` for unified capture after UI integration.

### Allowed Scope

- `test-support/baseline/capture-visuals.mjs`
- `.codex/refactor/baseline/visual/**`
- `.codex/refactor/evidence/IP-00/visual/**`
- `.codex/refactor/DECISION_QUEUE.md` only after a Stop Condition

### Protected Scope

- `test/baseline-visual.test.mjs`
- `public/index.html`, `public/app.js`, `public/styles.css`, and all other production UI
- Placeholder/generated mock images, production capture hooks, arbitrary fixed sleeps, test skips, assertion relaxation, and automatic golden acceptance

### Required Changes

1. Make the test-only capture path work with the current Electron development lifecycle demonstrated by `npm start dev`, without changing production UI or representing a synthetic drawing as the real Renderer.
2. Capture the ten states required by `test/baseline-visual.test.mjs` at an asserted 1440×900 content viewport, light appearance, device scale factor 1, reduced motion, `zh-CN`, and `Asia/Shanghai`.
3. Run the exact capture command twice. The second run must compare with the first and record ten identical hashes/dimensions in `manifest.json`.
4. Preserve fresh temporary `userData`, state isolation, deterministic fixtures, route/state readiness assertions, and cleanup on every success or failure path.

### Acceptance Tests

- `./node_modules/.bin/electron test-support/baseline/capture-visuals.mjs` succeeds twice.
- `node --test test/baseline-visual.test.mjs` passes unchanged.
- Human review confirms each PNG depicts the named state, has no contaminated overlay/dialog, and contains no secrets or user data.
- Capture duration and the two-run hash comparison are retained under `.codex/refactor/evidence/IP-00/visual/**`.

### Stop Conditions

- Real capture requires a production UI hook, relaxed assertion, placeholder, secret-bearing profile, or unsupported modification outside the path-exact scope.
- The current development lifecycle still cannot reach a capture-ready BrowserWindow after bounded diagnostic work.
- Two isolated runs are visually or byte unstable for an unexplained reason.

If a visual Stop Condition occurs, record/update `IP-0102-VISUAL-001` and preserve the evidence. Do not block unrelated IP-01/02 or pre-UI architecture work solely for this visual debt; do not claim the continuous package complete until it is resolved.

---

# IP-01: TypeScript, Vite, and Four-Process Skeleton

## Objective

Introduce a strict, deterministic TypeScript/Vite build skeleton for target Main, Preload, Shared, and React Renderer code while retaining the complete legacy application as the default and sole production workflow. IP-01 establishes build boundaries and a static opt-in modern shell only; it does not migrate business behavior or change the public bridge.

## Allowed Scope

- `package.json`, `package-lock.json`
- `electron-builder.yml` only to include deterministic `out/**` artifacts; app ID, product name, targets, icon, entitlements, signing, notarization, publishing, `extraResources`, and `asarUnpack` semantics remain unchanged
- `electron/main.mjs` only for an internal development renderer-entry resolver, exact-root navigation protection, and safe fallback to the existing legacy document
- `.github/workflows/ci.yml` and `.github/workflows/release.yml` only to build/typecheck/test the new sources before the existing validation/package steps; release triggers, permissions, signing secrets, targets, publishing, and artifact behavior remain unchanged
- `src/main/**`, `src/preload/**`, `src/shared/**`, `src/renderer/**`
- `tsconfig.json`, `tsconfig.*.json`
- `vite.config.ts`, `vite.*.config.ts`, `vitest.config.ts`
- New helpers under `scripts/refactor/**` and `test-support/refactor/**`
- New tests and fixtures under `test/refactor/ip-01/**`
- `.codex/refactor/baseline/contracts/build.json` only to append an IP-01 transition section while preserving every Gate-00 fact
- `test/baseline-contracts.test.mjs` only to keep the frozen build baseline executable while adding exact assertions for the approved additive transition; deleting or weakening an old assertion is forbidden
- `.codex/refactor/COMPATIBILITY_CONTRACT.md` only to append the implemented build/entry transition after validation; existing sections remain unchanged
- `.codex/refactor/adr/**` for the build/ESM-CJS/renderer-selection ADR
- `.codex/refactor/evidence/IP-01/**`
- `.codex/refactor/DECISION_QUEUE.md` only after a Stop Condition

Authorized dependencies:

- Runtime: `react`, `react-dom`
- Development: `typescript`, `vite`, `@vitejs/plugin-react`, `vitest`, `jsdom`, `@testing-library/react`, `@testing-library/dom`, `@testing-library/user-event`, `@testing-library/jest-dom`, `@types/node`, `@types/react`, `@types/react-dom`

Direct dependencies must be installed with exact versions and exact lockfile entries compatible with Node 22 and Electron 43.3.0. No other runtime, development, optional, peer-management, CSS, state, routing, testing, or Electron integration dependency is authorized.

## Protected Scope

- All recognition, provider, OCR, CSV, Project Library, SQLite, persistence, scanner, task, dialog, and environment implementations under `lib/**`, `public/**`, and `electron/**`, except the narrowly allowed `electron/main.mjs` entry-resolution change
- `electron/preload.cjs`, `electron/ipc-handlers.mjs`, all IPC channels/payloads/events, `window.electronAPI`, and `public/electron-bridge.js` until IP-02 begins
- Legacy `public/index.html`, `public/app.js`, `public/styles.css`, all current Worker modules, and generated pdf.js artifacts
- Existing tests and Gate-00 fixtures except the path-exact build-contract adaptation above and the separately authorized visual carryover
- SQLite schemas/pragmas, Project Library/version-1 formats, task JSON, CSV bytes/semantics, recognition behavior, native dialogs, environment names/defaults, and security flags
- App identity, icon, entitlements, release signing/notarization/publishing configuration, Git history/index, and user data

## Required Changes

1. Create project references or equivalent strict configurations for `src/main`, `src/preload`, `src/shared`, and `src/renderer`. Require `strict`, `noImplicitAny`, `noUncheckedIndexedAccess`, `exactOptionalPropertyTypes`, `noFallthroughCasesInSwitch`, and `forceConsistentCasingInFileNames`; keep `allowJs` off and do not use `skipLibCheck: true` to hide errors.
2. Emit deterministic, non-overlapping artifacts under the already ignored tree:
   - `out/main/**` from a NodeNext Main target;
   - `out/preload/**` as sandbox-compatible CommonJS;
   - `out/renderer/**` from Vite, with its own `index.html` and assets.
   No build may write into `electron/**`, `lib/**`, or `public/**`.
3. Establish source ownership:
   - `src/main/**`: typed target-main/build boundary only;
   - `src/preload/**`: typed Preload skeleton, not active and exposing no new global in IP-01;
   - `src/shared/**`: environment-neutral types/constants only, with no Electron, DOM, Node, secret, or service imports;
   - `src/renderer/**`: a static React migration shell with no project, settings, recognition, task, CSV, persistence, native-dialog, or network behavior.
4. Add an internal development command `electron:dev:modern` that builds the modern targets and launches `electron/main.mjs` with one fixed non-persisted command-line selector, `--slatesync-renderer=modern`. Ordinary `npm start`, `npm run dev`, and packaged startup must continue to load `public/index.html`.
5. In `electron/main.mjs`, honor that selector only when `app.isPackaged === false`. Resolve the modern HTML under `out/renderer`; allow navigation only inside the selected entry root; deny external windows/navigation exactly as today. If the modern entry is absent or its initial load rejects, log a bounded diagnostic and load the legacy document in the same window. Never bootstrap both Renderers.
6. Add additive scripts with these stable responsibilities (names may not be repurposed): `typecheck`, `build:main`, `build:preload`, `build:renderer`, `build:modern`, `test:node`, `test:modern`, `electron:dev:modern`, `test:electron:smoke`, and `validate:modern`. Keep `npm test` as the complete aggregate of the current Node suite and the new Vitest suite, retaining the existing native rebuild lifecycle. The Electron smoke command must be bounded, use temporary `userData`, manage the Electron native ABI explicitly when required, and leave the ordinary Node test path reproducible.
7. Ensure every packaging path builds modern outputs before electron-builder runs and add `out/**/*` to builder inputs. Update CI/release validation additively so fresh `npm ci` environments typecheck and build the new source; do not change release signing or publishing semantics.
8. Add smoke tests proving build-format and entry behavior, including legacy default, modern opt-in, missing/corrupt modern fallback, exact navigation roots, one Renderer per window, and packaged inclusion. Tests must use temporary output/user-data where applicable and must not require real user data or provider network calls.
9. Append—not rewrite—the transition portion of `build.json` and the Compatibility Contract so tests retain the Gate-00 build facts while recognizing the exact approved dependencies, scripts, outputs, and builder input.
10. Add/update comments where code establishes ESM/CJS boundaries, generated-output ownership, the development-only selector, fallback behavior, security roots, or the one-Renderer invariant. Remove comments made inaccurate by the entry resolver.

## Existing Behavior That Must Remain

- `npm start` and `npm run dev` rebuild the existing native dependency as needed and launch the current legacy app from the repository root.
- Default development and packaged windows load `public/index.html` with the current 1440×900 default, 960×600 minimum, title, CSP, context isolation, sandbox, web security, denied new windows, and file-navigation restriction.
- The legacy Renderer remains the only complete application and sole owner of all current UI orchestration.
- All 27 existing request operations, recognition progress behavior, business services, persistence, data formats, error behavior, dialogs, provider configuration, CSV Worker behavior, and legacy tests remain unchanged.
- Existing release targets and the owner-approved unsigned local build policy remain unchanged.

## Public Interfaces

- No user-facing or Renderer-to-Main public interface changes are authorized in IP-01.
- `window.electronAPI`, raw IPC names, request/event shapes, JSON/SQLite/CSV formats, environment variables, and ordinary CLI commands remain exactly as frozen at Gate 00.
- `--slatesync-renderer=modern` is an internal development/test selector, accepted only when Electron is not packaged. It is not an environment variable, preference, persisted setting, supported release CLI, or data contract.
- IP-01 may define internal build-time types, but it may not pre-emptively define Shared Contract v1 method names or business DTOs.

## Acceptance Tests

IP-01 local checkpoint must record every command and exit code:

1. `npm ci`
2. `npm run check`
3. `npm run typecheck`
4. `npm run test:modern`
5. `npm test`
6. `npm run build:modern`
7. `npm run test:electron:smoke`
8. `CSC_IDENTITY_AUTO_DISCOVERY=false npm run electron:build:dir`
9. `git diff --check`
10. A new IP-01 scope verifier against the recorded Batch start inventory

Additional assertions:

- TypeScript/Vitest contain zero skipped, todo, only, or auto-updated tests and no unapproved `any`, `@ts-ignore`, or `@ts-nocheck`.
- NodeNext Main output imports under Node 22; the Preload artifact is CommonJS; the Renderer bundle contains no `electron`, Node built-in, filesystem, database, provider-key, raw channel, or secret access.
- Ordinary startup loads only legacy; explicit development selection loads only the static React shell; missing/corrupt modern output falls back to only legacy.
- The unsigned unpacked app contains legacy inputs plus `out/main`, `out/preload`, and `out/renderer`, native SQLite, pdf.js, scripts/config, icon, and entitlements.
- `npm test` executes both suites. At this intermediate checkpoint, only the pre-approved visual-manifest failure may remain; every other Gate-00 and new test must pass. Luna must record the exact count rather than describing the suite as green.
- The amended baseline-contract test proves every original build fact plus the exact additive transition. It must fail if either the legacy facts or new facts drift.

## Performance Constraints

- The static modern shell's first meaningful paint is no more than 10% slower than the legacy empty-window baseline on the same machine and build mode. Record at least five comparable runs and report median values.
- The initial modern Renderer JavaScript, excluding source maps, is below 500 KiB uncompressed.
- The modern shell starts no Worker, database, provider request, filesystem operation, polling loop, or production background task.
- No window executes both legacy and modern Renderer code.
- The non-native focused `typecheck + test:modern + build:modern` sequence completes within 30 seconds on the review machine, excluding dependency installation, native rebuild, Electron download, and packaging.

## Stop Conditions

- The start HEAD or working-tree inventory differs from the recorded Gate-00 state in an unexplained way.
- A required dependency is outside the authorized list or incompatible with Node 22/Electron 43.3.0.
- Strict TypeScript requires weakening compiler settings, suppressing errors, duplicating business DTOs, or importing Node/Electron capabilities into Renderer code.
- The modern selector requires a new environment variable, persisted preference, production default change, dual boot, or modification to legacy HTML/application behavior.
- Packaging requires changing identity, entitlements, signing, notarization, publishing, release targets, or protected resource semantics.
- Any non-visual baseline/legacy test fails, or the build-contract transition can pass only by deleting/loosening a Gate-00 assertion.
- A production business module, IPC surface, data format, security flag, or user-data location must change.
- Any performance constraint is missed without a bounded implementation-only remedy.

## Deliverables

- Strict four-directory TypeScript/Vite skeleton and deterministic `out/**` builds
- Static opt-in React shell with safe legacy fallback and no business behavior
- Updated exact dependency lockfile, scripts, builder input, and additive CI/release validation
- IP-01 build, scope, Electron smoke, package-content, and performance evidence
- Build/ESM-CJS/selector ADR and appended build transition contract
- Synchronized architecture/lifecycle comments
- `PACKAGE: IP-01` local Completion Report with `ARCHITECTURE DEVIATIONS: NONE`

## IP-01 Local Checkpoint

IP-02 may begin in the same Luna task only when:

- all IP-01 acceptance items pass except the single documented visual carryover;
- no Decision Queue item is blocking;
- the IP-01 file list and diff are recorded;
- legacy remains the default complete application;
- Luna explicitly records `IP-01 LOCAL CHECKPOINT: PASS`.

---

# IP-02: Shared Contract v1 and Typed Preload

## Objective

Activate a typed, domain-grouped `window.slateSync` Preload gateway whose request methods return the Architecture-Invariant `Result<T>` envelope, while preserving all current legacy success values, thrown failures, listener cleanup, binary transfer, services, raw IPC channel names, and business behavior through exactly one temporary compatibility adapter.

## Allowed Scope

- `src/shared/contracts/**`, `src/shared/errors/**`, `src/shared/domain/**`
- `src/preload/**`
- `src/main/ipc/**` only for compile-time transport types or pure error-mapping utilities; it must not register duplicate handlers or own business services
- `electron/preload.cjs` only as the minimal bootstrap for the built typed Preload artifact
- `public/electron-bridge.js` as the only legacy compatibility adapter
- `package.json`, `package-lock.json`, TypeScript/Vite configs, and the IP-01 build helpers only where required to build/activate/test the typed Preload; no new dependency and no unrelated script change
- `test/electron-bridge.test.mjs` only to preserve and strengthen legacy adapter behavior
- New tests/fixtures under `test/refactor/ip-02/**`
- `.codex/refactor/baseline/contracts/ipc.json` only to append the IP-02 transition mapping; every Gate-00 method/channel/argument/event/security fact remains present
- `test/baseline-contracts.test.mjs` only to assert both the frozen legacy contract and new transition; no old assertion may be deleted or weakened
- `.codex/refactor/COMPATIBILITY_CONTRACT.md` only to append the implemented `electronAPI → slateSync` compatibility mapping and removal marker
- `.codex/refactor/adr/**` for Shared Contract v1/error/listener decisions
- `.codex/refactor/evidence/IP-02/**`
- `.codex/refactor/DECISION_QUEUE.md` only after a Stop Condition

No additional dependency is authorized in IP-02.

## Protected Scope

- `electron/ipc-handlers.mjs` and every Main business/service module: raw channel names, handler arguments, handler return values/rejections, validation, write/archive/library leases, persistence timing, recognition progress sending, and service calls remain unchanged
- `electron/main.mjs` except IP-01's already approved and provisionally frozen resolver
- All UI except `public/electron-bridge.js`; in particular `public/app.js`, `public/index.html`, `public/styles.css`, CSV/metadata/task/workflow modules, and modern feature implementation are protected
- Recognition algorithms/providers/OCR, CSV algorithms/Worker, SQLite, Project Library, tasks, settings/key stores, scanners, native dialogs, environment semantics, and all persisted/output formats
- Gate-00 fixtures/goldens/tests except the path-exact transition assertions and separately authorized visual carryover
- IP-01 Main/Renderer selection behavior, build boundaries, dependencies, performance ceilings, and evidence
- Release/signing/notarization/publishing configuration, Git history/index, and user data

## Required Changes

1. Define Shared Contract v1 in `src/shared/**` with one source of truth for DTOs, domain methods, `Result<T>`, `AppError`, event payloads, and the global `Window` declaration. Do not use `any`, duplicated declarations, or generic JSON bags for shapes that can be derived from current sanitized Main results, tests, and Gate-00 fixtures. An unprovable field is a Stop Condition, not permission to guess.
2. Use exactly this Renderer-facing namespace/method map:

| Namespace | Method | Current operation |
| --- | --- | --- |
| `app` | `getConfig()` | `getConfig` / `get-config` |
| `projects` | `list()` | `listProjects` / `list-projects` |
| `projects` | `getLibraryInfo()` | `getLibraryInfo` / `get-library-info` |
| `projects` | `importLibrary()` | `importProjectLibrary` / `import-project-library` |
| `projects` | `exportLibrary()` | `exportProjectLibrary` / `export-project-library` |
| `projects` | `changeLibraryLocation()` | `changeLibraryLocation` / `change-library-location` |
| `projects` | `create(request)` | `createProject` / `create-project` |
| `projects` | `load({ id })` | `loadProject` / `load-project` |
| `projects` | `update(request)` | `updateProject` / `update-project` |
| `projects` | `archive({ id })` | `archiveProject` / `archive-project` |
| `projects` | `restore({ id })` | `restoreProject` / `restore-project` |
| `projects` | `listScenarios({ projectId })` | `listScenarios` / `list-scenarios` |
| `projects` | `loadScenario({ projectId, id })` | `loadScenario` / `load-scenario` |
| `projects` | `importScenario({ projectId, profile })` | `importScenario` / `import-scenario` |
| `tasks` | `list({ projectId })` | `listTasks` / `list-tasks` |
| `tasks` | `load({ projectId, id })` | `loadTask` / `load-task` |
| `tasks` | `save({ projectId, task })` | `saveTask` / `save-task` |
| `tasks` | `delete({ projectId, id })` | `deleteTask` / `delete-task` |
| `recognition` | `getModels({ providerId, forceRefresh })` | `getModels` / `get-models` |
| `recognition` | `run(request)` | `recognize` / `recognize` |
| `recognition` | `onProgress(listener)` | `recognition-progress` event |
| `files` | `save({ defaultFilename, data })` | `saveFile` / `save-file` |
| `files` | `selectDirectory()` | `selectDirectory` / `select-directory` |
| `files` | `scanSlateDirectory(request)` | `scanSlateDirectory` / `scan-slate-directory` |
| `settings` | `saveProviderKey({ provider, apiKey })` | `saveProviderKey` / `save-provider-key` |
| `settings` | `getOcrSettings()` | `getOcrSettings` / `get-ocr-settings` |
| `settings` | `saveOcrSettings(request)` | `saveOcrSettings` / `save-ocr-settings` |
| `settings` | `checkOcr({ pythonPath })` | `checkOcr` / `check-ocr` |

All request methods return `Promise<Result<T>>`. `recognition.onProgress(listener)` returns an idempotent `() => void`, not a `Result` and not a cancellation function for recognition work.

3. Implement the typed Preload as the sole Renderer-to-Main gateway. Raw IPC channel strings exist only in Preload transport code. Do not expose `ipcRenderer`, generic invoke/send helpers, filesystem, SQLite, API keys, Electron objects, or arbitrary channels.
4. Keep all 27 Main handlers unchanged. At the Preload boundary, convert a fulfilled `ipcRenderer.invoke` to `{ ok: true, data }` and a rejected invocation to `{ ok: false, error }`. Before implementation, capture representative current Renderer-side rejection facts so the mapping and compatibility adapter preserve what Electron actually exposes.
5. Map errors deterministically without leaking stacks, secret values, raw Electron objects, or absolute paths:
   - `AppError.code` uses a safe existing string `error.code` when available;
   - otherwise an integer status becomes `HTTP_<status>`;
   - otherwise use `UNKNOWN`;
   - `message` preserves the current user-visible message after removing only Electron transport boilerplate proven by tests;
   - `retryable` uses an existing boolean when present, otherwise is true only for status 408, 429, 502, 503, or 504.
   This metadata must not add an automatic retry or change recognition timeout/retry behavior.
6. Implement progress subscription with one Electron listener per subscriber or an equally testable fan-out owned entirely by Preload. Each returned unsubscribe is idempotent, removes only its subscription, and prevents later callback delivery. Do not add `cancelRecognition`, alter the event payload, or reinterpret unsubscribe as request cancellation.
7. Preserve binary ownership: `files.save` accepts an exact `ArrayBuffer` or `ArrayBufferView`, sends only the view's byte range, and never converts binary to JavaScript number arrays or JSON. Avoid an extra full-size copy when the view already covers its backing buffer.
8. Activate the compiled Preload through `electron/preload.cjs` as a minimal bootstrap or equivalent single-source mechanism. The active page must expose only `window.slateSync`; `window.electronAPI` must no longer be created by Preload.
9. Rewrite `public/electron-bridge.js` as the one temporary legacy adapter. It calls `window.slateSync`, unwraps successful `Result<T>` values to the exact legacy raw values, reconstructs and throws compatible errors, adapts positional legacy arguments to typed request objects, and always unsubscribes recognition progress in `finally`. Mark this adapter for removal only in IP-08. Do not create a second adapter or global.
10. Add compile-time, isolated Preload, bridge-parity, event-lifecycle, error-redaction, transport-mapping, and exact-binary-range tests. Append the transition sections of `ipc.json` and the Compatibility Contract without rewriting the Gate-00 facts.
11. Add/update comments for the sole-gateway rule, Result/legacy-throw boundary, error redaction, listener ownership/idempotence, binary view ownership, and IP-08 removal marker. Remove stale comments claiming Preload exposes `electronAPI` directly.

## Existing Behavior That Must Remain

- The legacy UI retains all 27 request operations and receives the same successful values it currently expects.
- Legacy failures remain thrown from `public/electron-bridge.js` with equivalent user-visible message and recoverable `code`/`status` where the current Renderer receives them.
- Recognition inputs/results, progress payloads, page order, provider routing, timeout/retry/`AbortError`, write leases, diagnostic/task persistence timing, and absence of public recognition cancellation remain unchanged.
- Project Library operations, native dialogs, scanner behavior, OCR settings, task/scenario operations, CSV byte save, and typed-array subview range remain unchanged.
- Electron security remains `contextIsolation: true`, `nodeIntegration: false`, `sandbox: true`, `webSecurity: true`, and `allowRunningInsecureContent: false`; external navigation/windows remain denied.
- The legacy Renderer remains the default complete workflow, and the IP-01 modern shell remains static.

## Public Interfaces

The only new public Renderer gateway is:

```ts
type Result<T> =
  | { ok: true; data: T }
  | { ok: false; error: AppError };

interface AppError {
  code: string;
  message: string;
  retryable: boolean;
}

interface Window {
  slateSync: SlateSyncApi;
}
```

- `SlateSyncApi` contains only the six namespaces and exact methods in the Required Changes table.
- Request arguments are typed request objects except for methods with no arguments and `recognition.run(request)`.
- `recognition.onProgress` returns an idempotent unsubscribe.
- `window.electronAPI`, generic IPC functions, raw channels, raw filesystem/database/secrets, and a recognition-cancel API are forbidden.
- Raw Main channel names remain exactly those frozen in `.codex/refactor/baseline/contracts/ipc.json`; they are transport details, not Renderer interfaces.
- Shared Contract v1 becomes frozen only if Joint Gate 01-02 is `APPROVED`.

## Acceptance Tests

IP-02 and cumulative Batch validation must record every command and exit code:

1. `npm ci`
2. `npm run check`
3. `npm run typecheck`
4. `npm run test:node`
5. `npm run test:modern`
6. `npm test`
7. `npm run build:modern`
8. `npm run test:electron:smoke`
9. `CSC_IDENTITY_AUTO_DISCOVERY=false npm run electron:build:dir`
10. `node --test test/baseline-visual.test.mjs`
11. `git diff --check`
12. IP-01, IP-02, and cumulative Batch scope verifiers against the recorded start inventory

Required assertions:

- Typecheck proves Main/Preload/Renderer use one Shared Contract source with no `any`, duplicated DTO declarations, `@ts-ignore`, or `@ts-nocheck`.
- Isolated Preload inspection proves `window.slateSync` has exactly six namespaces and the approved method set; `window.electronAPI`, generic IPC, secrets, filesystem/SQLite handles, and unknown channels are absent.
- Every one of the 27 legacy operations has argument and success-shape parity coverage. Representative validation, not-found, busy, provider, timeout, dialog-unavailable, and unknown failures cover Result mapping, redaction, and legacy throw parity.
- Progress tests cover two simultaneous subscribers, independent unsubscribe, unsubscribe twice, success/failure cleanup in the legacy adapter, destroyed sender behavior through existing Main tests, and no callback after unsubscribe/unmount.
- Binary tests pass full buffers and non-zero-offset typed-array subviews and verify the exact bytes delivered to `save-file`, without number arrays or extra leading/trailing bytes.
- Bundle inspection finds no raw IPC channel string in `out/renderer/**`; channels are confined to `out/preload/**`.
- All Gate-00 recognition, CSV, persistence, environment, security, and build contracts pass. The transition-contract test proves both the original `electronAPI` mapping as historical compatibility input and the active `slateSync` mapping.
- If visual capture is completed in this batch, the ten real PNGs/stable manifest and unchanged visual test pass. Otherwise the exact known visual failure is documented and transferred; every non-visual test is green and no test is skipped/todo/only or weakened.
- Default legacy and opt-in modern Electron smoke tests pass with the typed Preload active; only one Renderer executes per window.
- The unsigned unpacked application starts, exposes only `slateSync`, contains all required old/new resources, and preserves native SQLite/pdf.js loading.

## Performance Constraints

- Result wrapping and legacy unwrapping add less than 1 ms median overhead to a resolved local gateway call across at least 1,000 measured calls, with methodology and raw summary retained.
- Passing a full `ArrayBuffer` adds no full-size copy; passing a subview copies at most its exact byte range once when required to prevent adjacent-byte transfer.
- Progress subscription retains no listener after all unsubscribes; repeated subscribe/unsubscribe cycles return Electron listener count and retained callback count to baseline.
- No new polling, timer, Worker, network request, database open, or global store is introduced by the gateway.
- IP-01 bundle-size and first-paint ceilings remain satisfied after Shared Contract/Preload activation.

## Stop Conditions

- Any of the 27 operations cannot be assigned to the approved namespace/signature without guessing or changing behavior.
- A current success/error DTO cannot be derived exactly from source/tests/fixtures without `any`, a generic bag, or an unapproved runtime dependency.
- Electron strips error facts required for legacy parity and preserving them would require changing Main handlers or raw IPC response shapes.
- More than one compatibility adapter/global, a generic invoke function, a raw channel, filesystem/SQLite handle, API key, or Electron object would be exposed.
- Error mapping changes legacy-visible messages/status, introduces automatic retry, hides a regression, or leaks secrets/absolute paths/stacks.
- Listener cleanup requires adding recognition cancellation or changing Main event semantics.
- Exact binary range cannot cross Preload without number-array conversion or an avoidable full-buffer copy.
- Any protected Main service, IPC handler, business behavior, persistence/output format, security setting, IP-01 boundary, or release configuration must change.
- A baseline test can pass only by deletion, skip, todo/only, fixture auto-update, assertion relaxation, or historical fact removal.
- Any cumulative non-visual validation has a new failure. The known visual carryover alone follows the Owner Policy Update and is not a batch Stop Condition.

On a Stop Condition, append a correctly formatted `IP-0102-*` entry to `.codex/refactor/DECISION_QUEUE.md`, stop the entire batch, and report `BATCH STATUS: BLOCKED`. Do not improvise a new interface or continue to IP-03.

## Deliverables

- Shared Contract v1 types/declarations and exact six-namespace API
- Active sandbox-compatible typed Preload with Result/AppError mapping
- Single legacy compatibility adapter in `public/electron-bridge.js`, marked for IP-08 removal
- Exact IPC transition inventory and appended Compatibility Contract mapping
- Compile-time, transport, parity, redaction, listener, binary, Electron smoke, and package-content tests
- Shared Contract/error/listener ADR
- IP-02 scope, test, build, package, and performance evidence
- Synchronized architecture/compatibility/lifecycle comments
- Completed real Gate-00 visual baseline and stable evidence, or an explicit unchanged-debt handoff to `IP-03-08-CONTINUOUS.md`
- `PACKAGE: IP-02` Completion Report and cumulative Batch Completion Report with `ARCHITECTURE DEVIATIONS: NONE`

## Batch Completion and Handoff

Luna stops after IP-02. The Batch is ready for Sol only when:

1. Both IP sections report `COMPLETED` and all changed files map to the correct IP or visual carryover scope.
2. Every required command has exact retained output/exit status.
3. Every non-visual test is green; visual evidence is either complete or transferred unchanged under the Owner Policy Update.
4. The unsigned directory build and legacy/modern smoke tests pass.
5. No blocking Decision Queue entry or unexplained architecture deviation remains.
6. Comments and appended compatibility/build/IPC documentation match the actual implementation.
7. Luna reports `BATCH: IP-01 + IP-02`, `BATCH STATUS: READY FOR JOINT REVIEW`, and `NEXT BATCH ENTERED: NO`.

Only Sol may write `GATE-01-02.md`, freeze Shared Contract v1, authorize corrections, or admit `IP-03-08-CONTINUOUS.md` after the non-visual foundation requirements pass.
