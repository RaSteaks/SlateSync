# IP-0102-C01: Correct Shared Contract and Runtime Validation

STATUS: **COMPLETED AND APPROVED HISTORICAL CORRECTION — NOT EXECUTABLE**  
Gate 01-02 closed this package. Its “next authorization none” and “do not enter
IP-03” lines applied only while correction review was pending; current authority
is `IP-03-08-CONTINUOUS.md` version `2026-08-21.2`.

## Package Metadata

- Parent: `IP-0102` (`IP-01 + IP-02`)
- Gate: Gate 01-02 re-review
- Trigger: `reviews/GATE-01-02.md` (`CHANGES REQUIRED`)
- Baseline HEAD: `c7dafa4d972e5eb7be61f00e2b546d6826e70c87`
- Next phase authorization: none
- Packaging: unsigned directory build only; Apple signing/notarization is not required

Execute only this Correction Package. Do not implement continuous-package
features, migrate business UI, edit this package/review, stage, commit, push,
switch branches, or enter IP-03.

## Objective

Derive an exact Shared Contract v1 from current Main behavior, enforce
`window.slateSync` as the sole production gateway, replace self-referential
smoke/parity checks with production-runtime evidence, make Node/Electron native
ABI lifecycles deterministic, and keep generated build artifacts under
`out/**`.

## Allowed Scope

- `package.json`, `package-lock.json` only for scripts/native lifecycle; no dependency changes
- `.github/workflows/ci.yml`, `.github/workflows/release.yml` only for required validation ordering
- `electron/main.mjs` only for a behavior-preserving, testable version of the approved resolver/fallback/navigation boundary
- `electron/preload.cjs` only for a behavior-preserving minimal-bootstrap correction
- `public/electron-bridge.js` only to retain one adapter over `slateSync` and remove the alternate transport
- `src/main/**`, `src/preload/**`, `src/shared/**`, `src/renderer/**`
- `tsconfig.json`, `tsconfig.*.json`, `vite.*.config.ts`, `vitest.config.ts`
- `test/electron-bridge.test.mjs`, `test/baseline-contracts.test.mjs`
- `test/refactor/ip-01/**`, `test/refactor/ip-02/**`, `test-support/refactor/**`
- `.codex/refactor/baseline/contracts/build.json` and `ipc.json` only to correct additive transition facts
- `.codex/refactor/COMPATIBILITY_CONTRACT.md` only to append corrected facts
- `.codex/refactor/adr/ADR-IP-0102-TYPED-GATEWAY.md`
- `.codex/refactor/evidence/IP-01/**`, `IP-02/**`, and `IP-0102/**`
- `.codex/refactor/DECISION_QUEUE.md` only to append evidence or a Stop Condition

Correction may remove generated root `tsconfig.*.tsbuildinfo` and generated
`src/shared/**/*.js`, `*.js.map`, `*.d.ts`, and `*.d.ts.map`. Authored
TypeScript source must remain.

## Protected Scope

- `electron/ipc-handlers.mjs` and every Main channel, payload, return, and rejection shape
- `lib/**`; recognition/OCR/provider, SQLite, Project Library, task, scenario, settings/key, dialog, scanner, and persistence implementations
- `public/**` except `public/electron-bridge.js`; legacy HTML/CSS/app, CSV Worker, metadata, and workflow modules
- Gate-00 fixtures/tests/PNGs/manifest and visual capture harness
- app identity, targets, icon, entitlements, signing, notarization, publishing, release triggers/permissions/secrets, and builder resources
- Master Plan, Architecture Invariants, Execution Guide, Migration Matrix, package files, and review files
- Git history/index, real user data/Project Libraries, credentials, and provider network calls

## Required Changes

### 1. Correct Shared Contract v1

1. Build an operation-by-operation matrix from live handlers, service returns, frozen fixtures, and compatibility tests.
2. Define separate exact DTOs for Scenario summary/full profile, scanner stats, canonical slate metadata, Resolve CSV table/format/sparse edits, and task snapshots.
3. Include compatibility fields/variants such as `missingMetadataKeys`, `scenarioMatch`, and supported usage-key variants.
4. Remove generic JSON bags wherever current source/fixtures prove the shape. Truly opaque fields require a focused ownership comment and evidence.
5. Keep the exact six namespaces, methods, request objects, raw channels, `Result<T>`, `AppError`, and progress unsubscribe.
6. Add compile-time representative success fixtures for all 27 operations using the single Shared Contract source.
7. If a shape needs guessing, `any`, a generic bag, dependency, or Main/data-format change, trigger a Stop Condition.

### 2. Enforce the sole gateway and legacy parity

1. Remove every `globalThis.electronAPI` read and typed/legacy dual-call path from production bridge code.
2. Update unit fixtures to inject a complete fake `slateSync`; historical `electronAPI` remains only an inventory fact.
3. Test all 27 adapter mappings with representative success values and exact arguments/raw legacy results.
4. Cover validation, ENOENT/not-found, `PROJECT_BUSY`/`LIBRARY_BUSY`, provider/HTTP timeout, dialog unavailable, and unknown failures.
5. Capture actual Electron rejected-`invoke` facts. Preserve safe message/code/status/retryability without stacks, paths, secrets, retry, or cancellation.
6. Complete two-subscriber, independent/repeated unsubscribe, no-late-delivery, adapter success/failure cleanup, and destroyed-sender tests.
7. Test full `ArrayBuffer`, full typed-array identity/no-copy, and subview exact-range copy without number arrays or adjacent bytes.

### 3. Prove the production Electron runtime

1. With temporary `userData` and bounded cleanup, exercise production `electron/main.mjs` for default legacy, dev modern, missing modern fallback, and corrupt/failed modern load fallback.
2. Prove one Renderer, denied external windows/navigation, and exact selected file root.
3. Exercise production `electron/preload.cjs` and assert only `slateSync` with six namespaces; no `electronAPI`, generic invoke, Electron/filesystem/database/secret handle.
4. Start the unsigned unpacked app with a bounded temporary profile and prove legacy default, typed Preload, native SQLite, pdf.js, and resources.
5. Controllers may live only under `test-support/refactor/**`; no production hook, test IPC, new global, environment selector, or preference.
6. Retain pure selector tests, including packaged-mode refusal of modern, but do not substitute them for runtime smoke.

### 4. Correct native lifecycle, output ownership, and release validation

1. Make `npm run test:node` reproducible after Electron rebuild/package. ABI 137 belongs only to the current Node test runner; ABI 148 belongs to Electron. Do not restore a Node Web service.
2. Isolate native artifacts or use narrowly named hooks that prepare the required ABI and restore Node ABI in `finally`.
3. End validation with green Node and aggregate suites so handoff state matches the report.
4. Make release validation run strict typecheck and modern build before electron-builder without changing release policy.
5. Put every `.tsbuildinfo` and emitted compiler artifact below ignored `out/**`; remove root/source generated artifacts and prove they do not return.
6. Keep Main/Shared/Preload/Renderer outputs deterministic and non-overlapping.

### 5. Correct scope, tests, and performance evidence

1. Provide separate read-only IP-01, IP-02, and cumulative scope modes with exact path matrices; broad prefix exemptions are forbidden.
2. Record exact per-IP changed paths and pre-existing Architect/Gate-00 artifacts separately. Reject generated files outside `out/**`.
3. Ensure transition tests observe executable/generated facts; changing inventory and assertion together is insufficient.
4. Measure comparable production legacy/modern paths after layout/paint readiness for five runs. A custom-window `loadURL`-to-`querySelector` metric is not first meaningful paint.
5. Rerun the 1,000-call gateway benchmark after correction.
6. Synchronize comments, ADR, Compatibility appendix, inventories, Decision Queue evidence, and completion reports with executable evidence.

## Existing Behavior That Must Remain

- Legacy stays the ordinary/packaged complete app; modern remains static, development-only, and opt-in.
- All 27 Main channels and current inputs/returns/rejections remain unchanged.
- Recognition progress, timeout/retry/AbortError/concurrency/provider routing, leases, dialogs, scanner, OCR, tasks/scenarios, and no-cancel behavior remain.
- SQLite/Project Library/task/CSV formats and semantics, Worker ownership, environment defaults, security flags, and user-data locations remain.
- Gate-00 visual files/test remain byte-identical.
- Apple signing/notarization is not required; existing configuration is protected.

## Public Interfaces

- The only target interface remains the exact six-domain `window.slateSync` map from IP-0102.
- Requests return `Promise<Result<T>>`; progress returns an idempotent unsubscribe.
- DTO corrections describe current values only. No method/channel/payload/Main return may change.
- `electronAPI`, generic IPC, raw Renderer channels, Electron/filesystem/database/secrets, and cancellation APIs are forbidden.
- Shared Contract v1 remains provisional until Gate re-review approves it.

## Acceptance Tests

Run in order and retain exact output, exit status, environment, and duration:

1. `npm ci`
2. `npm run check`
3. `npm run typecheck`
4. `npm run test:node`
5. `npm run test:modern`
6. `npm test`
7. `npm run build:modern`
8. corrected production `npm run test:electron:smoke`
9. `CSC_IDENTITY_AUTO_DISCOVERY=false npm run electron:build:dir`
10. corrected bounded unsigned packaged-app startup smoke
11. `node --test test/baseline-visual.test.mjs`
12. IP-01 scope verifier
13. IP-02 scope verifier
14. cumulative IP-0102 scope verifier
15. `git diff --check`
16. `npm run test:node` after Electron/package checks
17. `npm test` as the final executable state

Required assertions:

- No failure, skip, todo, only, `any`, `@ts-ignore`, or `@ts-nocheck`.
- All 27 operations have compile-time success DTOs, exact transport/adapter parity, and the error matrix.
- Scenario, scanner, task, CSV, metadata, and usage variants match source/fixtures.
- Production Main selection/fallback/navigation, one Renderer, Preload, sole global, and packaged startup pass from temporary profiles.
- Binary/listener ownership stays within approved copy/retention bounds.
- Fresh typecheck/build creates no artifact outside `out/**`.
- Gate-00 recognition/CSV/persistence/environment/security/build/visual tests remain unchanged and green.

## Performance Constraints

- Production modern first meaningful paint is at most 10% slower than comparable legacy across five runs.
- Modern Renderer JS stays below 500 KiB uncompressed, excluding maps.
- Gateway/adapter overhead stays below 1 ms median over 1,000 calls.
- Full buffer is not copied; a subview copies its exact range at most once.
- Listener counts return to baseline; no new polling/Worker/network/database/background task is introduced.
- Focused non-native typecheck/test/build stays below 30 seconds.

## Stop Conditions

Stop and append a Decision Queue entry if:

- an exact DTO/error fact needs guessing, `any`, generic bag, Main/data changes, or dependency;
- Electron strips a required legacy-visible fact that cannot be preserved without Main changes;
- production smoke needs a production hook/channel/global, real user data, provider network, security relaxation, or alternate selector;
- ABI reproducibility needs supported-version or packaging changes;
- a protected path/behavior/format/security/release setting must change;
- a test must be removed, skipped, todo/only, relaxed, or auto-updated;
- a performance ceiling fails without a bounded implementation-only remedy;
- branch/HEAD or unexplained user changes appear.

Apple signing/key absence is not a Stop Condition. Use the unsigned directory command.

## Deliverables

- Exact Shared Contract v1 candidate and six-domain typed Preload
- Sole `slateSync` adapter with complete parity evidence
- Production Main/Preload/default/fallback/navigation/package smoke evidence
- Deterministic Node/Electron ABI lifecycle and out-only build artifacts
- Strict release validation, complete 27-operation tests, and separate scope evidence
- Correct production-path performance evidence
- Updated comments, contracts, ADR, Decision Queue evidence, and reports
- Completion Report containing:
  - `BATCH: IP-01 + IP-02 CORRECTION C01`
  - `BATCH STATUS: READY FOR GATE 01-02 RE-REVIEW`
  - `ARCHITECTURE DEVIATIONS: NONE`
  - `NEXT BATCH ENTERED: NO`

Stop after the correction report. Do not enter `IP-03-08-CONTINUOUS.md`.
