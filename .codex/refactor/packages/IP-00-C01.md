# IP-00-C01: Complete and Strengthen the Baseline Freeze

STATUS: **COMPLETED HISTORICAL CORRECTION — NOT EXECUTABLE**  
Its original stop/next-phase text records the old execution boundary and does
not override `IP-03-08-CONTINUOUS.md` version `2026-08-21.2`.

## Package Metadata

- Parent package: `IP-00`
- Review Gate: `Gate 00` re-review
- Trigger: `.codex/refactor/reviews/GATE-00.md` (`CHANGES REQUIRED`)
- Baseline commit: `c7dafa4d972e5eb7be61f00e2b546d6826e70c87`
- Branch: `codex/react-architecture-refactor`
- Next phase authorization: none

Execute only this Correction Package. Do not implement IP-01, modify production code, stage, commit, push, or resolve Gate 00 yourself.

## Objective

Complete the missing visual baseline and make every claimed IP-00 inventory/golden artifact an executable regression boundary, while preserving the unchanged Electron application and all current compatibility behavior.

## Exact Issues

1. Ten required visual PNGs and `manifest.json` are missing; `npm test` fails.
2. The visual harness uses non-temporary runtime state, captures sequentially contaminated UI states, does not create the required prepared multi-page workspace state, does not prove a 1440×900 content viewport, and omits required manifest metadata.
3. Build/Electron/environment/IPC/provider inventories and SQLite indexes, unique constraints, foreign keys, and pragmas are not completely compared to live source/runtime facts.
4. CSV tests do not consume a byte-level output golden manifest and do not freeze the required Worker/subview/lifecycle boundaries.
5. Recognition tests do not deterministically prove out-of-order completion, a valid page-concurrency override, or monotonic required progress phases; fixture records are duplicated in test code.
6. Persistence fixtures/inventory are not fully consumed and do not prove all required Project Library, source-of-truth, migration, ordering/projection, and permission behavior.
7. Validation files contain summaries rather than exact command evidence; the baseline timestamp is not an ISO UTC timestamp; the exact directory-build limitation lacks its own Decision Queue entry if still applicable.
8. The capture harness contains an unused duplicate bridge implementation, and the scope verifier accepts filename suffixes through overly broad prefix matching.

## Expected Architecture

- Production ownership and all public/runtime contracts remain exactly at commit `c7dafa4d...`.
- Baseline tests call current public exports or structurally parse current configuration; inventories are reviewed expected values, never regenerated by tests.
- Binary output remains `ArrayBuffer`/transferable and is compared at the exact byte-range boundary.
- Visual capture loads unchanged `public/index.html`, production Renderer, and production CSS through a test-only preload. Each state starts from a known isolated state and has explicit readiness assertions.
- Runtime capture files, Chromium state, caches, and generated preload code live in a newly created temporary directory and are removed in `finally`. Only reviewed PNGs and their manifest are written to the baseline evidence directory.
- No second source of truth, compatibility adapter, production hook, new dependency, or temporary abstraction is introduced.

## Allowed Files

Only these existing IP-00 paths may be created or changed by the Implementer:

- `.codex/refactor/DECISION_QUEUE.md` — append observations only when a Stop Condition is met; do not mark a decision resolved
- `.codex/refactor/baseline/BASELINE_MANIFEST.json`
- `.codex/refactor/baseline/validation/preflight.txt`
- `.codex/refactor/baseline/validation/final.txt`
- `.codex/refactor/baseline/contracts/*.json`
- `.codex/refactor/baseline/persistence/schema.json`
- `.codex/refactor/baseline/visual/**`
- `test/fixtures/baseline/recognition/**`
- `test/fixtures/baseline/csv/**`
- `test/fixtures/baseline/persistence/**`
- `test-support/baseline/capture-visuals.mjs`
- `test-support/baseline/verify-scope.mjs`
- `test/baseline-contracts.test.mjs`
- `test/baseline-recognition.test.mjs`
- `test/baseline-csv.test.mjs`
- `test/baseline-persistence.test.mjs`
- `test/baseline-visual.test.mjs`

The Architect-owned `.codex/refactor/packages/IP-00.md`, `.codex/refactor/packages/IP-00-C01.md`, and `.codex/refactor/reviews/GATE-00.md` may be recognized by the read-only scope verifier, but the Implementer must not edit them.

## Protected Files

Everything outside Allowed Files is protected. In particular:

- `electron/**`, `lib/**`, `public/**`, `scripts/**`, `assets/**`, `build/**`, `bin/**`
- Every pre-existing `test/*.test.mjs`
- `package.json`, `package-lock.json`, `electron-builder.yml`, `slatesync.config.json`, `.env.example`, `.gitignore`, `.github/**`
- `.codex/refactor/MASTER_PLAN.md`, `ARCHITECTURE_INVARIANTS.md`, `EXECUTION_GUIDE.md`, `COMPATIBILITY_CONTRACT.md`, and `MIGRATION_MATRIX.md`
- Both Architect package files and `reviews/GATE-00.md`
- Git index/history, ignored user files, real Electron `userData`, and real Project Libraries
- All public interfaces, IPC/data formats, schemas, defaults, algorithms, visuals, error text, packaging rules, and production comments

## Required Changes

### 1. Produce valid isolated visual evidence

- Remove the unused duplicate `bridgeScript()` and keep one reviewed test-fixture/preload source.
- Create runtime `userData`, generated preload, and any cache beneath a fresh OS temporary directory. Clean it in `finally`, including failure paths. Do not place `.capture-user-data` or generated preload files beneath `.codex/refactor/baseline/visual/`.
- Use a 1440×900 **content** viewport and assert the captured PNG dimensions. Keep DPR 1 where supported, light appearance, reduced animation, fixed locale/timezone where supported, Electron 43.3.0, and no network/credentials.
- Start every screenshot from a fresh window or implement an explicit, asserted reset that closes overlays/dialogs, clears prior tabs/progress, and restores the intended route. State from one capture must not affect another.
- Build the exact ten IP-00 states. In particular, `workspace-ready` must show a prepared multi-page slate and visible recognition settings without substituting a completed result; progress must be visible only in `recognition-progress`; result and CSV captures must not retain the progress overlay; dialogs must not overlap one another.
- Wait for fonts/layout and explicit route/state selectors. Do not use arbitrary fixed sleeps as evidence of readiness.
- Record state, route, fixture ID, viewport, DPR, platform, Electron version, exact capture command, dimensions, byte count, SHA-256, appearance, and reduced-motion status in `manifest.json`.
- Run capture twice in the same supported environment. All ten dimensions and hashes must match. Retain only the reviewed second/identical evidence set.
- Manually inspect all ten PNGs for correct state, complete rendering, synthetic-only data, and absence of real paths/credentials.

### 2. Make contract and persistence inventories executable

- Make `baseline-contracts` read and validate every contract inventory, including `build.json`.
- Compare the exact preload method/channel set and relevant Main handler/event surface, argument wrappers, listener cleanup, and absence of a public cancellation channel. Additive or renamed live methods/channels must fail.
- Structurally compare package entrypoint/scripts/engine/dependency versions and electron-builder identity/files/resources/unpack/mac targets/icon/entitlements without a brittle whole-file snapshot and without adding a YAML dependency.
- Compare BrowserWindow dimensions/security/navigation/renderer entry facts to live source or safely executable exports rather than asserting inventory values against themselves.
- Verify every `.env.example` name plus source-only runtime variables, and compare executable defaults/ranges/enums/secret boundaries wherever current exports or safe structural parsing permit.
- Query temporary library/project databases and compare pragmas, tables, columns, indexes, indexed-column order/direction, uniqueness, and foreign-key actions to `schema.json`.
- Consume the library/project/settings/task and migration recipe fixtures. Prove Project Library create/list/open/export/validate/copy without format rewrite; project isolation; SQLite task authority plus compatible JSON snapshots; one-time legacy JSON and `slatesync.sqlite` migration with intact source; CSV preview round-trip; more-than-50 ordering/projection behavior; and POSIX owner-only modes where supported. Reuse current public APIs and temporary directories; do not commit an opaque SQLite database as sole evidence.

### 3. Complete recognition golden behavior

- Drive provider responses from `pages.json`; remove duplicated hard-coded page records.
- Use controlled promises/barriers, not wall-clock delays, to make later pages finish before earlier pages and prove returned record order/source pages remain correct.
- Add a valid page-concurrency override case and assert the configured maximum active requests without timing assertions.
- Assert the expected progress phase sequence for the fixture and that every percentage is numeric, capped, and monotonically non-decreasing.
- Preserve the existing timeout-shaped `AbortError`, retry/exhaustion, non-timeout failure, empty/failed page, listener cleanup, and no-public-cancel assertions. Do not invent cancellation.

### 4. Create real byte-level CSV goldens

- Add expected output files or a reviewed canonical output manifest containing encoding, BOM, newline, delimiter, byte length, SHA-256, and decoded table for every deterministic output case. Tests must read this evidence and compare the exact output; they must never auto-update it.
- Cover UTF-8 with/without BOM, UTF-16LE/BE BOM, LF/CRLF, alternate delimiter/quoted comma/quote/newline, original row/column preservation, multiple scenes, width/non-truncation, configured Comments, match/missing/incomplete/conflict behavior, metadata backfill, sparse edits, and standalone output.
- Exercise actual Worker output transfer semantics sufficiently to prove the transferred result is an `ArrayBuffer` for the exact typed-array subview byte range, retained metadata across requests, explicit clear, task-error classification, pending-request rejection on termination, and Renderer fallback only for infrastructure failure.
- Keep fixtures synthetic and small; do not convert binary data into JSON number arrays.

### 5. Correct evidence and scope verification

- Record an ISO-8601 UTC capture timestamp in `BASELINE_MANIFEST.json` and make status accurately distinguish completed and blocked evidence.
- Replace paraphrased validation summaries with command-delimited exact stdout/stderr, exit status, and environment qualifiers for every IP-00 command. Do not claim a command passed when only an altered command passed.
- Run the exact `npm run electron:build:dir`. If it still fails solely for signing/toolchain/platform reasons, append a separate correctly formatted Decision Queue entry under Stop Condition 11 and stop that subtask. An unsigned supplemental run may be recorded but must not be mislabeled as the exact command.
- Update `verify-scope.mjs` so exact files are exact matches, directory prefixes end at a directory boundary, untracked files are included, and only the three known Architect documents are exempted. It must remain read-only.
- Update focused comments wherever isolation, canonicalization, binary ownership, deterministic scheduling, or state reset changed. Remove stale/duplicated comments and avoid syntax narration.

## Forbidden Workarounds

- No production hook, production CSS/Renderer edit, IPC change, schema/data-format change, dependency, lockfile edit, builder edit, or environment-default change.
- No placeholder/generated mock screenshot that does not render the actual current Renderer and CSS.
- No skipped/todo/only tests, conditional visual pass, missing-file allowance, hash auto-update, broad truthiness assertion, or removal/relaxation of an existing assertion.
- No reuse of real credentials, network, userData, Library, personal path/data, or production media.
- No fixed delay used to prove concurrency or UI readiness.
- No acceptance of an unresolved baseline gap. If the supported GUI/toolchain is unavailable, keep the package blocked and report it; do not declare completion.
- No editing this package, the Gate review, IP-00, compatibility contract, migration matrix, or any production file.

## Acceptance Tests

All commands must complete with zero failures unless a Stop Condition is explicitly queued, in which case Gate 00 remains blocked:

1. Run `node --check` on every new/changed `.mjs`/`.js` test and test-support file.
2. Run these focused files twice, including visual validation:

   ```text
   node --test test/baseline-recognition.test.mjs test/baseline-csv.test.mjs test/baseline-contracts.test.mjs test/baseline-persistence.test.mjs test/baseline-visual.test.mjs
   ```

   Both runs must have identical test counts, 0 failures, 0 skipped, and 0 todo.

3. Run the visual capture command twice. Verify ten identical SHA-256 values and dimensions across the two runs, then perform human review of all ten images.
4. Run:

   ```text
   npm run check
   npm test
   npm run electron:build:dir
   node test-support/baseline/verify-scope.mjs
   git diff --check
   git status --porcelain=v2 --branch
   git diff --name-status
   git diff --cached --name-status
   git ls-files --others --exclude-standard
   ```

5. `npm test` must report 0 failures/skips/todo. Scope verification must report no path outside C01 Allowed Files plus the three read-only Architect artifacts.
6. Demonstrate that deliberate in-memory mutations of a loaded build inventory value, CSV golden hash/byte length, schema index/FK fact, recognition expected order/progress, and visual hash each cause the corresponding assertion to fail. Do not modify committed evidence merely to perform this check.
7. Confirm Protected Files remain byte-identical to baseline commit and no real/ignored runtime data was added.

## Performance Constraints

- Zero production runtime work and zero new dependency.
- Non-visual focused tests remain under 20 seconds on the baseline machine.
- Each complete ten-state visual capture run remains under 60 seconds and exits Electron cleanly.
- Non-visual fixtures remain below 5 MiB total; visual PNGs remain below 20 MiB total.
- No avoidable ArrayBuffer duplication or JSON-serialized binary.

## Stop Conditions

Stop the entire correction if branch/HEAD/upstream changes, unexpected user changes appear, a pre-existing test fails independently of C01, or any required fix needs Protected Files, a new dependency, a public/data-format change, real data/network, or weakened tests.

Stop the affected subtask and append a Decision Queue entry if:

- a supported Electron GUI/offscreen session still cannot produce the real ten-state capture;
- two correctly isolated, event-ready captures differ;
- exact directory build fails for an external signing/toolchain/platform reason;
- a schema/contract fact cannot be safely observed without guessing;
- fixture size/performance limits cannot be met without dropping required behavior.

An unresolved blocking decision means `STATUS: PARTIAL` or `BLOCKED`; it is not Gate approval.

## Deliverables

- Corrected files only within Allowed Files
- Ten PNGs and complete visual `manifest.json`
- Exact updated preflight/final validation evidence
- Decision Queue append only if a Stop Condition occurs
- Standard Completion Report with exact changed paths, comment updates, all validation results, compatibility statement, Decision Queue status, `ARCHITECTURE DEVIATIONS: NONE`, and known limitations

Stop after the Completion Report. Do not enter IP-01.
