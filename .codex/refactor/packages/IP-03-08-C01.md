# IP-03-08-C01: Complete and Reconcile the Final-Review Handoff

STATUS: **CORRECTION REQUIRED — FINAL REVIEW NOT ADMITTED**  
TRIGGER: `.codex/refactor/reviews/FINAL-IP-03-08.md`  
PARENT: `.codex/refactor/packages/IP-03-08-CONTINUOUS.md` version
`2026-08-21.2`  
IMPLEMENTER: GPT-5.6 Luna XHigh  
REVIEWER AFTER COMPLETION: GPT-5.6 Sol High

This is a handoff/evidence/governance correction only. It does not authorize a
production implementation change, a test relaxation, or a new refactor phase.

## Objective

Produce the missing truthful Continuous Completion Report, reconcile the
machine-readable authority and Decision Queue with the actual completed state,
restore the required root project instruction/plan records, and rerun the
complete final acceptance matrix so Sol can independently begin the real
IP-03—08 Final Review.

## Allowed Scope

- `.codex/refactor/evidence/IP-03-08/CONTINUOUS_COMPLETION_REPORT.md`
- `.codex/refactor/evidence/IP-03-08/CHECKPOINT-LEDGER.md` only to append or
  correct current rerun facts; historical command outcomes must remain intact
- new, command-specific evidence files below
  `.codex/refactor/evidence/IP-03-08/final-handoff/**`
- `.codex/refactor/CURRENT_STATE.json` only to record the truthful handoff
  phase and the exact Completion Report path after every acceptance item passes
- `.codex/refactor/README.md` only to mirror that current phase
- `.codex/refactor/DECISION_QUEUE.md` only to update the top current resolution
  index and append correction evidence; do not rewrite historical entries
- root `AGENTS.md` and root `AGENT.md` only to preserve the supplied repository
  instruction and record the current project/review plan without expanding
  implementation authority
- generated validation outputs already owned by existing scripts under
  `out/**`, `dist/**`, `storybook-static/**`, and `test-results/**`
- ignored native build output produced by the existing ABI lifecycle scripts

No other path is authorized. If a final command exposes a production defect,
stop and request a separate finding-specific correction package; do not fix it
under this package.

## Protected Scope

- all production source under `src/**`, `electron/**`, `public/**`, and
  `lib/**`
- `package.json`, `package-lock.json`, all TypeScript/Vite/Vitest/Playwright/
  Storybook configuration, `electron-builder.yml`, and `.github/workflows/**`
- Architecture Invariants, Shared Contract v1, Compatibility Contract,
  Migration Matrix, Traceability Matrix, Cleanup Manifest, and all ADRs
- every test, fixture, assertion, golden, screenshot, capture script, smoke
  controller, performance script, migration reader, and package-inspection
  script
- Gate-00, Gate-01/02, IP-00, IP-01, IP-02, and IP-0102 frozen evidence
- recognition/OCR/provider/CSV/SQLite/Project Library/task/version-1 behavior,
  security/navigation, app identity, icons, entitlements, signing,
  notarization, publishing, secrets, credentials, and user data
- Git history/index and branch state

## Required Changes

### 1. Restore auditable repository instructions

1. Add root `AGENTS.md` containing the supplied repository instruction that
   code edits require synchronized comments and that the root project plan is
   recorded in `AGENT.md`.
2. Add root `AGENT.md` with the current architecture/refactor plan, the sole
   active correction package, protected production scope, validation order,
   and the stop-for-Sol handoff. It must not authorize new production work.

### 2. Create the Continuous Completion Report

Create
`.codex/refactor/evidence/IP-03-08/CONTINUOUS_COMPLETION_REPORT.md` using every
field required by the parent package:

- package and `STATUS: READY FOR FINAL REVIEW`;
- start HEAD/status/diff inventory and Gate 01-02 baseline;
- all nine completed workstreams;
- implemented behavior and complete changed-file inventory;
- comments updated and stale comments removed;
- tests added/updated without weakening;
- checkpoint ledger and complete final validation;
- performance/memory and visual/accessibility evidence;
- migration/data safety and compatibility;
- cleanup manifest and Decision Queue disposition;
- `ARCHITECTURE DEVIATIONS: NONE`, or a new blocking Decision Queue entry if
  that statement cannot truthfully be made;
- known limitations;
- `GIT COMMIT PERFORMED: NO`;
- `READY FOR SOL FINAL REVIEW: YES`.

Do not copy claims from the current Ledger without rechecking the referenced
raw evidence and rerunning the command where this package requires a fresh
handoff result. Record exact command, environment, start/end or duration, exit
code, counts, and evidence path.

### 3. Reconcile current authority and decisions

1. After and only after every acceptance item passes, change
   `CURRENT_STATE.json` and the authority README to a truthful
   `IP-03-08_READY_FOR_FINAL_REVIEW` phase and add the exact Completion Report
   path. Preserve package version/hash and all Git mutation prohibitions.
2. Update the Decision Queue's top current resolution index to include
   `IP-03-08-SAFETY-ISOLATION-001` with its later audited/quarantined
   disposition and `IP-03-08-CONTINUOUS-CLEANUP-001` as waiting for Sol Final
   Review. Distinguish historical `OPEN/BLOCKING` text from the current index.
3. Do not close either item on Sol's behalf and do not declare the refactor
   complete.

### 4. Preserve evidence truth and scope

1. Preserve the Gate-approved dirty worktree and all existing evidence.
2. Record pre- and post-correction porcelain status and exact changed paths.
3. Prove that correction-authored changes are limited to this package's
   Allowed Scope and that production/source/test/lockfile contents did not
   change.
4. Do not rewrite historical failures, auto-accept screenshots/goldens, or
   reuse the quarantined default Project Library.

## Acceptance Tests

Run the existing commands without changing their implementation or assertions.
Retain raw output, exit code, duration, relevant environment, and evidence path:

1. `node .codex/refactor/verify-current-state.mjs` before correction and again
   after authority reconciliation; both must report version `2026-08-21.2` and
   the authorized SHA-256.
2. Clean `npm ci`.
3. `npm run check`.
4. `npm run typecheck`.
5. Complete Node/baseline suite.
6. Complete modern Vitest/component suite.
7. Aggregate `npm test`.
8. Main, Preload, Renderer/modern, and Storybook builds.
9. Electron development, production, explicit legacy/fallback, and packaged
   smoke using isolated temporary `userData` and explicit temporary
   `libraryPath`.
10. Playwright Electron E2E: two fresh runs and one copied version-1 Library
    run, with no live data, network, credentials, or fixed sleeps.
11. `CSC_IDENTITY_AUTO_DISCOVERY=false npm run electron:build:dir`.
12. Gate-00 recognition, CSV, persistence, contract, and visual compatibility
    tests.
13. Exact CSV differential/byte suite plus deliberate one-byte mutation
    failure.
14. Worker transfer, memory, lifecycle, task/infrastructure failure, and exact
    subview suite.
15. 10,000-row virtualization, DOM-row, rerender, keyboard, scroll, sparse
    edit, export, FPS/long-task, and memory measurements.
16. Two deterministic visual captures and hash comparison plus human review of
    light/dark, 1440×900/960×600, compact, error, focus, keyboard, ARIA,
    contrast, and reduced motion.
17. Copied version-1 migration and source-integrity validation.
18. Package contents, security/navigation, secret isolation, native resources,
    and Node/Electron ABI automatic recovery.
19. Cleanup Manifest verifier, orphan audit, lockfile integrity, exact scope
    verifier, and `git diff --check`.
20. A final post-Electron Node/baseline suite and aggregate `npm test` to prove
    the Node ABI and test state are restored.

Acceptance requires zero failure, skip, todo, only, relaxed assertion,
auto-accepted golden, placeholder screenshot, fixed sleep, production test
hook, real secret/network, or active user-data dependency. Apple Development
signing, notarization, and publication credentials are not required.

## Stop Conditions

Stop and append a structured blocking Decision Queue entry if:

1. any production source, test, fixture, lockfile, configuration, contract,
   migration, ADR, Cleanup Manifest, or protected evidence must change;
2. any required command fails or cannot produce trustworthy raw evidence;
3. a result conflicts with the current Ledger, Completion Report, package
   threshold, Architecture Invariant, or Compatibility Contract;
4. the handoff cannot truthfully state `ARCHITECTURE DEVIATIONS: NONE` and
   `READY FOR SOL FINAL REVIEW: YES`;
5. validation would touch the quarantined default Library, live user data,
   credentials, provider network, signing identity, Git index/history, or
   require a production test hook/fixed wait;
6. scope expands beyond the exact Allowed Scope above.

Do not convert a failing result into a passing report and do not implement the
underlying fix. Preserve evidence and return for a finding-specific package.

## Deliverables

- root `AGENTS.md` and `AGENT.md` with reconciled instructions/plan
- complete
  `.codex/refactor/evidence/IP-03-08/CONTINUOUS_COMPLETION_REPORT.md`
- raw final-handoff evidence and updated Checkpoint Ledger references
- current authority phase/report-path reconciliation
- complete current Decision Queue resolution index
- pre/post exact scope and worktree inventory proving no production/test/
  lockfile change
- a Luna handoff ending with `READY FOR SOL FINAL REVIEW: YES`

Stop after these deliverables. Do not modify
`.codex/refactor/reviews/FINAL-IP-03-08.md`, do not enter another phase, and do
not stage, commit, push, reset, clean, or switch branches.
