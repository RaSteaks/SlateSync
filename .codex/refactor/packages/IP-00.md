# IP-00：Baseline Freeze

STATUS: **COMPLETED HISTORICAL PACKAGE — NOT EXECUTABLE**  
Current authority: `../README.md`, `../CURRENT_STATE.json`, and
`IP-03-08-CONTINUOUS.md` version `2026-08-21.2`.

## Package Metadata

- Package: `IP-00`
- Phase: Baseline Freeze
- Review Gate: `Gate 00`
- Implementer: GPT-5.6 Luna XHigh
- Architect baseline commit: `c7dafa4d972e5eb7be61f00e2b546d6826e70c87`
- Architect baseline branch: `codex/react-architecture-refactor`
- Upstream at package completion: `origin/codex/react-architecture-refactor` (`+0/-0`)
- Working tree before this package was written: clean; no tracked, staged, or untracked changes
- Known architect change after package generation: this new file, `.codex/refactor/packages/IP-00.md`
- Concurrent Git observation: inspection began on `codex/electron-only`; at 2026-08-18 13:46:02 +0800 an external checkout moved the worktree to `codex/react-architecture-refactor`. Both branches pointed to the same baseline commit, so no source content changed. IP-00 must execute on the latter refactor branch and must not switch branches itself.
- Local inspection environment: macOS Darwin 24.6.0 arm64, Node `v24.13.0`, npm `11.6.2`
- CI compatibility environment: Node 22 on Ubuntu; macOS release matrix is arm64/x64

This package is an implementation specification only. Generating this file does **not** execute IP-00 and does not authorize IP-01 or any production refactor.

The repository root did not contain a physical `AGENTS.md` at package-generation time. The active repository instruction supplied by the execution environment is authoritative and must be preserved: **after editing code, add or update the necessary code comments**. For test harness code added by this package, comments must explain non-obvious isolation, compatibility, ownership, and determinism boundaries rather than restating syntax.

## Objective

Create a deterministic, reviewable safety baseline for the current Electron-only application without changing production behavior, public interfaces, data formats, runtime configuration, dependencies, or packaging.

The baseline must make later migration regressions observable in these areas:

1. Recognition normalization, multi-page ordering, progress, timeout/retry, error, and currently exposed cancellation behavior.
2. Resolve CSV decode, merge, normalization, sparse edits, encode, Worker binary transfer, and byte-level output semantics.
3. Task snapshot, SQLite, Project Library, project settings, legacy migration, and machine settings formats.
4. Electron Main/Preload/Renderer IPC surface, native dialog behavior, security settings, entrypoints, packaged inputs, generated resources, and environment-variable semantics.
5. Key legacy UI routes and states at a fixed Electron viewport.
6. The exact pre-migration Git state and a procedure that prevents existing user work from being overwritten or cleaned.

IP-00 is evidence work. It must not “improve,” reorganize, type, migrate, or replace existing implementation.

## Repository Baseline Observed by the Architect

The Implementer must verify these observations before changing any Allowed Scope file:

- `package.json` uses `electron/main.mjs` as the entrypoint and `public/index.html` as the renderer document loaded by Main.
- The current Renderer is a JavaScript DOM application in `public/app.js`; it receives backend access through `electron/preload.cjs` as `globalThis.electronAPI`.
- BrowserWindow is 1440×900 by default, minimum 960×600, with `contextIsolation: true`, `nodeIntegration: false`, `sandbox: true`, and external navigation/window creation denied.
- SQLite is the source of truth. Library registry data is stored in `library.sqlite`; each project owns `project.sqlite`; JSON task/diagnostic files remain compatibility snapshots.
- Project Library and project format versions are both `1`; the portable folder suffix is `.slatesync-library`.
- Recognition defaults are request timeout 180000 ms, timeout retries 1, page concurrency 2, and process-level concurrent recognitions 1. Configured ranges are timeout 30000–3600000 ms, retries 0–3, page concurrency 1–6, and concurrent recognitions 1–16.
- Timeout is enforced with `AbortSignal.timeout`. `TimeoutError` and a timeout-shaped `AbortError` are retryable; final timeout is normalized to a readable 504 provider error. There is currently no public `cancel-recognition` IPC channel or explicit Renderer cancellation API; progress listener removal after an invocation is the exposed cleanup behavior. Freeze this observed behavior—do not invent cancellation support in IP-00.
- Resolve CSV work currently uses a long-lived module Worker for decode/merge/normalize/encode, retains the decoded table in the Worker, and transfers output as `ArrayBuffer`. A Renderer fallback exists for Worker infrastructure failure.
- Existing source validation is `npm run check`; existing test validation is `npm test` (`node --test`, with native SQLite rebuild in `pretest`). There are 26 current test files and broad existing coverage; they must all remain enabled and unmodified.
- Current installed direct versions are Electron 43.3.0, electron-builder 26.15.3, better-sqlite3 12.11.1, pdfjs-dist 5.6.205, undici 8.10.0, and `@electron/rebuild` 4.2.0.
- `electron-builder.yml` packages `electron/**/*`, `lib/**/*`, `public/**/*`, and `package.json`; adds `scripts/`, `bin/`, and `slatesync.config.json` as resources; and unpacks scripts, bin, and the better-sqlite3 native binary.
- `public/vendor/pdfjs/` is generated by `postinstall` and ignored by Git. `bin/vision-ocr` is generated and ignored. The builder icon container and macOS entitlements are tracked build inputs.
- The current theme is light-only (`color-scheme: light`). IP-00 must not fabricate a dark baseline.

If any observation is no longer true at implementation start, apply the relevant Stop Condition instead of silently updating the package.

## Allowed Scope

Only the following paths may be created or changed:

- `.codex/refactor/COMPATIBILITY_CONTRACT.md`
- `.codex/refactor/MIGRATION_MATRIX.md`
- `.codex/refactor/baseline/**`
- `test/fixtures/baseline/**`
- `test-support/baseline/**`
- New files matching `test/baseline-*.test.mjs`
- `.codex/refactor/DECISION_QUEUE.md`, only to append a correctly formatted Decision Queue entry after a Stop Condition is met

Additional restrictions inside Allowed Scope:

- Do not modify this package file.
- Prefer new files. Do not rewrite unrelated baseline evidence from another package if it appears during execution.
- Test fixtures must be synthetic, minimal, deterministic, reviewable, and contain no real production media, project data, paths, credentials, tokens, or personal information.
- Test tools may write runtime output only beneath a newly created temporary directory or ignored `dist/`; they must never use the operator’s real Electron `userData`, Project Library, `.env`, Downloads, or Documents directories.
- Visual capture tooling must use a test-only preload/stub and isolated temporary `userData`. It must not add test hooks to production modules.
- New code must contain focused comments for non-obvious sandboxing, fixture canonicalization, dynamic-field normalization, Worker transfer, or UI determinism decisions.

## Protected Scope

Everything not listed in Allowed Scope is protected. In particular, do not modify, replace, regenerate as tracked content, move, or delete:

- `electron/**`
- `lib/**`
- `public/**`, including `public/app.js`, `public/index.html`, `public/styles.css`, Worker files, and generated `public/vendor/pdfjs/**`
- `scripts/**`
- `assets/**`
- `build/**`
- `bin/**`
- All existing `test/*.test.mjs`
- Existing `test-support/synthetic-production-day.mjs`
- `package.json` and `package-lock.json`
- `electron-builder.yml`
- `slatesync.config.json`
- `.env.example` and any local `.env`
- `.github/**`
- `.gitignore`
- `README.md`, `LICENSE`, and OCR requirement/setup files
- `.codex/refactor/MASTER_PLAN.md`
- `.codex/refactor/ARCHITECTURE_INVARIANTS.md`
- `.codex/refactor/EXECUTION_GUIDE.md`
- `.codex/refactor/packages/IP-00.md`
- Git index, branch history, tags, remotes, stashes, ignored user files, and any pre-existing user changes
- Runtime behavior, IPC channel names, preload API names, schemas, persistence contents, environment defaults/ranges, packaging inputs, visuals, and error text except as observed and documented by new baseline evidence

Forbidden operations include `git reset --hard`, `git checkout --`, `git clean`, blanket formatters, dependency installation/update, lockfile regeneration, deleting or skipping tests, and writing test data into a real user library.

## Required Changes

### 1. Freeze and protect the start state

Create `.codex/refactor/baseline/BASELINE_MANIFEST.json` and a human-readable `.codex/refactor/baseline/WORKTREE.md` that record:

- Full HEAD SHA, branch, upstream, ahead/behind counts, and UTC capture time.
- Exact `git status --porcelain=v2 --branch` captured before implementation.
- A categorized list of pre-existing tracked, staged, untracked, and ignored-but-required build inputs.
- SHA-256 values for at least `package.json`, `package-lock.json`, `electron-builder.yml`, `slatesync.config.json`, and `.env.example`.
- Local Node/npm/OS/architecture versions and resolved direct dependency versions.
- The distinction between the clean state observed before package generation and the known architect-created `IP-00.md` change.
- A statement that no user change was cleaned, overwritten, staged, committed, or stashed.

Add a test-only scope verifier under `test-support/baseline/` that fails when the IP-00 diff includes a path outside Allowed Scope. It must account for untracked files and must not alter Git state.

### 2. Create the compatibility contract

Create `.codex/refactor/COMPATIBILITY_CONTRACT.md`. It must describe the current contract rather than the target architecture and must include source references for:

- Recognition provider/model routing, result normalization, field widths, multi-scene values, take-status mapping, multi-page ordering/inheritance, high-accuracy stages, progress events, timeout/retry/error behavior, process/page concurrency, OCR optional/required fallbacks, and the current absence of an explicit cancellation API.
- Renderer-to-preload API names and corresponding IPC channels, argument/return shapes, recognition progress listener lifecycle, raw `ArrayBuffer` file-save path, and native file/directory/Project Library dialogs.
- CSV input encodings/BOM/newline/delimiter behavior; column/order preservation; matching; multi-scene export; Scene/Shot/Take width rules; Comments allowlist/configuration; sparse edits; standalone export; Worker ownership; transferables; and Renderer fallback semantics.
- SQLite pragmas, filenames, tables, indexes, JSON-in-SQLite columns, source-of-truth rules, file permissions where supported, snapshot compatibility, task list projection/order, default task shape, and legacy JSON/SQLite migration behavior.
- Project Library directory tree, suffix, default location behavior, `library.json`, `project.json`, `library.sqlite`, per-project `project.sqlite`, format versions, default project, archive semantics, import/export validation, switch/relaunch behavior, and machine-level `settings.json` fields.
- Environment-variable names, defaults, valid ranges/enums, secret/public exposure rules, development versus packaged `.env` location, workflow config path behavior, and runtime provider-key override behavior.
- Electron window/security/navigation behavior, app lifecycle, dev/packaged resource roots, package entrypoint, builder include/exclude/resource rules, CI/release Node versions, and generated pdf.js/Vision OCR resources.

Do not copy secret values from a local environment. Variable names and documented defaults are sufficient.

### 3. Add file-backed recognition golden fixtures

Under `test/fixtures/baseline/recognition/`, add synthetic request/provider-response/expected-result fixtures. Add one or more new `test/baseline-recognition*.test.mjs` files that consume them without network or local OCR dependencies.

The fixtures and tests must freeze at least:

- Scene/Shot/Take padding, wider-number preservation, full-width digits, Chinese numerals, scene suffix capitalization, multi-scene canonical separator, and take-status mapping.
- Multi-page input where requests complete out of order but returned records remain in page order and carry correct `sourcePage` values.
- Merged Scene/Shot inheritance across rows/pages and warning/error behavior for an empty or failed page.
- Page concurrency default `2` and valid override behavior without asserting wall-clock timing.
- A timeout-shaped `AbortError` that retries exactly once by default and succeeds on the second attempt.
- Exhausted timeout retry producing the current readable 504 message/attempt count.
- A non-timeout failure remaining non-retryable under the current behavior.
- Recognition progress event phases and monotonic/capped percentages for the fixture workflow.
- Preload/Renderer listener cleanup and the fact that no public cancel-recognition channel exists.

Reuse public exports and injected fake fetch/OCR implementations. Do not call a real model provider, spawn OCR, embed a real slate image, or assert nondeterministic IDs/durations without canonicalizing them.

### 4. Add byte-level CSV golden fixtures

Under `test/fixtures/baseline/csv/`, add small synthetic source and expected-output files plus a readable fixture manifest. Cover:

- UTF-8 with and without BOM as supported today, UTF-16LE with BOM, and UTF-16BE with BOM.
- LF/CRLF preservation as currently implemented, delimiter and quoted-cell handling, embedded comma/quote/newline cases, and original header/column/row order.
- Resolve merge for multiple scenes, zero padding and non-truncation, configured Comments markers, unmatched/incomplete/conflicting rows, metadata backfill, sparse cell edits, and standalone export.
- Exact output bytes where the current implementation is deterministic; otherwise compare a canonical byte manifest containing encoding, BOM, newline, delimiter, byte length, SHA-256, and decoded table.
- Worker output as transferable `ArrayBuffer`, exact byte range handling for typed-array subviews, retained metadata between requests, clear behavior, task-error classification, termination, and Renderer fallback only for infrastructure failure.

Add new `test/baseline-csv*.test.mjs` tests. Golden output changes in later packages must fail loudly and require Sol review; tests must not auto-update snapshots.

### 5. Freeze persistence and Project Library formats

Under `test/fixtures/baseline/persistence/`, add synthetic version-1 task, CSV preview snapshot, settings, Project Library, project manifest, and legacy migration fixtures. Add new `test/baseline-persistence*.test.mjs` tests.

Create normalized machine-readable schema inventories beneath `.codex/refactor/baseline/persistence/` by opening temporary databases through the current public store APIs and querying SQLite metadata. Include:

- Database filenames and pragmas.
- Every table, column, type, nullability/default/primary-key property, index, indexed-column order, unique constraint, and foreign-key action for library and project databases.
- Canonical `library.json` and `project.json` keys and version values.
- Canonical `project_meta` keys.
- Full `createTask()` field set and a version-1 completed-task example, including project ownership/settings snapshot and CSV preview fields.
- Machine `settings.json` keys and defaults.
- Expected portable directory tree and owner-only file/directory modes on POSIX systems.

Tests must prove:

- A version-1 library and project created by current code can be listed, opened, exported, validated, and copied without format rewriting.
- Separate projects cannot read one another’s tasks.
- Task JSON in SQLite is authoritative while JSON snapshots remain compatible.
- A legacy JSON task and legacy `slatesync.sqlite` migrate once into the default project, gain project ownership where current code does so, and leave the source intact.
- CSV preview table, sparse edits, metadata, warnings, and filenames round-trip.
- More than 50 tasks are retained and ordering/projection behavior remains current.
- Dynamic paths, IDs, timestamps, SQLite page layout, and WAL state are canonicalized; do not commit opaque platform-specific SQLite database binaries as the sole schema evidence.

### 6. Freeze IPC, environment, and build inputs

Create machine-readable inventories beneath `.codex/refactor/baseline/contracts/` for:

- Preload API names and IPC channel names.
- BrowserWindow security/navigation settings and renderer entry document.
- Environment variables from `.env.example`, source-code-only runtime variables, defaults, ranges/enums, secret classification, and dev/packaged source location.
- `package.json` entrypoint/scripts/engine/direct dependency versions.
- `electron-builder.yml` app identity, files, resources, unpack rules, mac targets/architectures, icon, and entitlements.
- Tracked versus generated packaging inputs, including pdf.js copies and Vision OCR binary.
- CI and release validation/build commands.

Add `test/baseline-contracts*.test.mjs` tests that compare live configuration/source exports to the inventories where behavior is executable. Static facts that cannot be imported safely must be parsed structurally or documented with file/line references; avoid brittle whole-source text snapshots.

The inventory must include all `.env.example` names and also internally assigned runtime variables such as `SLATESYNC_PROJECT_DIR`. It must explicitly distinguish release-only signing/publishing secrets from application runtime settings.

### 7. Capture deterministic visual baselines

Add a test-only Electron capture harness and preload stub beneath `test-support/baseline/`. The harness must:

- Launch with an isolated temporary `userData` directory and no real credentials or network.
- Load the existing `public/index.html` and production CSS/Renderer unchanged.
- Supply deterministic synthetic Project Library, project, task, recognition result, CSV table, and settings responses through a test-only preload.
- Use Electron 43.3.0, a 1440×900 content viewport, device scale factor 1 where supported, light appearance, reduced animation, fixed locale/timezone where supported, and wait for fonts/layout plus explicit application state—not arbitrary sleeps.
- Save full-window PNGs beneath `.codex/refactor/baseline/visual/` and a JSON manifest with state name, route, fixture ID, viewport, DPR, platform, Electron version, capture command, dimensions, byte size, and SHA-256.

Capture these minimum states:

1. Project Library with active and archived projects.
2. Workspace with no slate selected.
3. Workspace with prepared multi-page slate and recognition settings visible.
4. Recognition progress overlay.
5. Result detail with warnings and editable recognition rows.
6. Resolve CSV preview with matched, edited, missing, and anomaly row states.
7. Project Settings.
8. Global Settings.
9. New Project dialog.
10. Local OCR setup dialog.

Regular `npm test` must validate that every required image is present, is a valid PNG with the declared 1440×900 dimensions, and matches its manifest hash. It must not perform cross-platform pixel comparison. Gate 00 performs human visual review of the images. If deterministic capture requires a production hook, production code edit, new dependency, or real user data, stop instead of weakening isolation.

### 8. Create the migration matrix

Create `.codex/refactor/MIGRATION_MATRIX.md` mapping each current implementation area to its current owner, compatibility evidence, protected behavior, and earliest authorized future package. Include at least:

- Electron lifecycle/window/navigation
- Preload and IPC handlers
- Project Library and project runtime
- SQLite/task/diagnostic/scenario persistence
- Recognition orchestration and OCR
- Slate/PDF preparation
- Renderer workflow/page routing
- Metadata scanning
- CSV Worker/client/algorithms
- CSV table/edit/export UI
- Settings and secrets
- Styles and visual routes
- Builder/release inputs

IP-00 must mark all production areas as “baseline only / not migrated.” It must not design IP-01 implementation details beyond citing the phase already authorized by the Master Plan.

## Existing Behavior That Must Remain

- All existing tests and syntax checks remain unchanged, enabled, and passing.
- Recognition provider behavior, prompts/schemas, field normalization, high-accuracy audit/review, PDF and image paths, progress, timeout/retry, error text/status, OCR fallback, page concurrency, process concurrency, and persistence timing remain unchanged.
- A timeout-shaped `AbortError` remains compatible. No cancellation channel is added or removed.
- CSV algorithms and byte semantics remain unchanged, including BOM/encoding/newline/delimiter, header/row preservation, match/conflict rules, Comments rules, manual edits, standalone output, Worker ownership, transferables, and fallback behavior.
- SQLite schemas, filenames, pragmas, JSON payload shapes, migration markers, file modes, snapshots, task ordering, and source-of-truth rules remain unchanged.
- Project Library/package suffix, manifests, version values, default project/location, project isolation, archive behavior, import/export validation, and relaunch-on-switch remain unchanged.
- Machine settings, key storage, environment variables, config defaults/ranges, public secret redaction, file dialog behavior, and resource path resolution remain unchanged.
- BrowserWindow flags, navigation restrictions, lifecycle, preload surface, IPC channels, Renderer entrypoint, current UI routes/styles/states, and packaged artifacts remain unchanged.
- No current user file or Git change is removed, hidden, staged, committed, stashed, or overwritten.

## Public Interfaces

IP-00 may observe and freeze but may not modify these interfaces:

- `globalThis.electronAPI` exposed by `electron/preload.cjs`.
- Every `ipcRenderer.invoke`/`ipcMain.handle` channel and the `recognition-progress` event.
- Recognition request, progress, success, task-persistence, and thrown-error shapes.
- Recognition environment variables and workflow configuration semantics.
- `ArrayBuffer` file-save and CSV Worker message contracts.
- Resolve CSV accepted input and emitted byte formats.
- `library.json`, `project.json`, `settings.json`, task/diagnostic JSON snapshots, and SQLite schemas/content conventions.
- `.slatesync-library` directory layout and import/export behavior.
- `package.json` scripts/entrypoint, Electron window behavior, and electron-builder input/output contract.

No new runtime-facing interface may be introduced. Test-only helpers must live entirely under Allowed Scope and must be named/documented as non-production APIs.

## Acceptance Tests

### A. Preflight evidence

Run before any implementation edit and retain exact output in `.codex/refactor/baseline/validation/preflight.txt`:

```text
git rev-parse HEAD
git status --porcelain=v2 --branch
git diff --name-status
git diff --cached --name-status
git ls-files --others --exclude-standard
node --version
npm --version
npm ls --depth=0
npm run check
npm test
```

Expected at package completion: branch is `codex/react-architecture-refactor`, HEAD is `c7dafa4d972e5eb7be61f00e2b546d6826e70c87`, upstream is `origin/codex/react-architecture-refactor` at `+0/-0`, and the only known new path is `.codex/refactor/packages/IP-00.md`. If branch, HEAD, upstream, or other status differs, do not overwrite anything—apply Stop Conditions.

### B. New focused baseline tests

Run all newly added baseline test files explicitly with `node --test`. The exact file list must be written to the validation report. At minimum, focused runs must cover recognition, CSV, persistence/Project Library, contracts/build/environment, visual manifest validation, and scope verification.

All focused tests must:

- Run offline.
- Use temporary directories.
- Be deterministic across two consecutive runs.
- Fail when a golden file or inventory is changed without an intentional fixture update.
- Avoid skipped/todo/only tests and broad assertions that merely check truthiness.

### C. Full repository validation

After implementation, run and retain exact commands/results:

```text
npm run check
npm test
npm run electron:build:dir
git diff --check
git status --porcelain=v2 --branch
git diff --name-status
git diff --cached --name-status
git ls-files --others --exclude-standard
```

Also run `node --check` on every new `.mjs`/`.js` test-support file because the current fixed `npm run check` list will not discover new files automatically.

The unpacked Electron directory build must succeed without editing builder configuration. Verify that Main, Preload, Renderer, lib, package metadata, scripts, config, pdf.js artifacts, Vision OCR resource (when present in the prepared build environment), native SQLite module, icon, and entitlements follow the current builder contract. Do not publish, sign, notarize, create a release, or run a destructive clean.

### D. Visual evidence

Run the documented test-only Electron capture command twice from the same clean baseline environment. Image dimensions and hashes must be stable across the two runs. Review all ten required PNGs for complete rendering, no real credentials/paths/data, and correct fixture state. Record any platform-specific font/render caveat in the visual manifest; do not normalize it by changing production CSS.

### E. Scope and compatibility review

Gate 00 must be able to verify from the final diff that:

- Every changed path is in Allowed Scope.
- Every Protected Scope file has identical Git content to the package start.
- Existing tests were not modified, removed, skipped, relaxed, or snapshot-updated.
- Golden fixtures assert meaningful values/bytes/schema rather than mirroring implementation constants without execution.
- No secret, production image, real Project Library, absolute personal path, generated `node_modules`, `dist`, `bin`, or `public/vendor` artifact was committed.
- Comments were added/updated for non-obvious new test code as required by the repository instruction.
- `ARCHITECTURE DEVIATIONS` is `NONE`.

## Performance Constraints

- IP-00 must add zero production runtime work and zero shipped production dependency.
- No runtime dependency or devDependency may be added.
- Baseline tests must not call network services, start OCR engines, or use fixed delays to prove concurrency.
- Non-visual focused baseline tests should complete in under 20 seconds on the local baseline machine, excluding the existing full suite/native rebuild.
- Visual capture should complete in under 60 seconds on the local baseline machine and must terminate Electron/Worker processes cleanly.
- Synthetic non-visual fixture data should remain below 5 MiB total; visual PNGs should remain below 20 MiB total. Exceeding either limit requires a Decision Queue entry with measured justification.
- CSV fixtures must be large enough to prove byte/transfer semantics but must not introduce a benchmark-sized file.
- Do not duplicate large ArrayBuffers or serialize binary data as number arrays in test tooling when the contract under test is transferable binary ownership.

## Stop Conditions

When a condition is met, do not improvise. Append an entry to `.codex/refactor/DECISION_QUEUE.md` using the Master Plan format, stop the affected task, and continue only independent work that does not obscure the problem.

Stop the entire package when:

1. Implementation starts from a branch other than `codex/react-architecture-refactor`, a different HEAD than the package baseline, or an unexpected upstream/ahead-behind state, unless Sol first regenerates or explicitly re-authorizes IP-00 against that state.
2. Unexpected tracked, staged, or untracked changes exist beyond this architect-created package, or any existing user change cannot be safely distinguished and preserved.
3. `npm run check` or `npm test` fails before IP-00 changes. A failing pre-migration baseline may not be recorded as green and may not be “fixed” in IP-00.
4. Completing the baseline requires modifying any Protected Scope file, existing test, dependency, lockfile, production entrypoint, IPC surface, schema, algorithm, environment default, packaging rule, or UI.
5. A fixture or screenshot can only be produced from real credentials, network access, real production data, or the operator’s real Electron userData/Project Library.
6. Observed runtime behavior conflicts with Architecture Invariants or the Master Plan in a way that requires choosing which behavior is authoritative.
7. Any test must be skipped, loosened, deleted, made order-dependent, or auto-updated to pass.
8. Baseline artifacts contain secrets, personal paths/data, copyrighted production media, or an opaque binary whose provenance cannot be reviewed.

Stop only the affected subtask when:

9. Deterministic visual capture requires production test hooks, a new dependency, or production CSS/Renderer edits. Preserve other baseline work and queue the visual-capture decision.
10. Two identical visual captures in the same environment produce different dimensions or hashes after event-based readiness and animation suppression are correctly implemented.
11. The unpacked Electron build fails for an external signing/toolchain/platform reason after the existing configuration has been checked without modification.
12. A schema/inventory fact cannot be observed through a safe temporary instance or structural parsing and would otherwise require guessing.
13. Fixture/performance size limits cannot be met without losing the behavior the fixture is meant to prove.

The following always require a Decision Queue entry and Sol authorization: adding any dependency; changing public IPC/contracts; changing ownership; changing persistence/CSV/recognition semantics; introducing a compatibility layer; or accepting a baseline gap.

## Deliverables

The Completion Report must list the exact paths produced. Expected deliverables are:

- `.codex/refactor/COMPATIBILITY_CONTRACT.md`
- `.codex/refactor/MIGRATION_MATRIX.md`
- `.codex/refactor/baseline/BASELINE_MANIFEST.json`
- `.codex/refactor/baseline/WORKTREE.md`
- `.codex/refactor/baseline/contracts/**`
- `.codex/refactor/baseline/persistence/**`
- `.codex/refactor/baseline/visual/**` including ten minimum PNGs and their manifest
- `.codex/refactor/baseline/validation/preflight.txt`
- `.codex/refactor/baseline/validation/final.txt`
- `test/fixtures/baseline/recognition/**`
- `test/fixtures/baseline/csv/**`
- `test/fixtures/baseline/persistence/**`
- New `test/baseline-*.test.mjs` files
- `test-support/baseline/**` including scope verification and isolated visual capture tooling
- `.codex/refactor/DECISION_QUEUE.md` only if a Stop Condition occurs
- Standard IP completion report with `COMMENTS UPDATED`, validation evidence, `COMPATIBILITY`, Decision Queue status, and `ARCHITECTURE DEVIATIONS: NONE`

Do not commit, stage, push, open a PR, generate Gate 00, edit this package, or begin IP-01. Stop after the IP-00 Completion Report and wait for Sol Gate 00 review.

## Gate 00 Review Focus

Sol must inspect actual artifacts and rerun representative validation. Approval requires:

- Worktree/user-change protection is credible and complete.
- Golden tests exercise current behavior rather than reimplement it.
- Recognition, CSV, persistence, environment, IPC, builder, and visual boundaries are specific enough to detect later regressions.
- No Protected Scope change or production behavior change occurred.
- All pre-existing and new tests pass without weakening.
- The Electron directory build succeeds or an external-only build limitation is explicitly queued and resolved.
- Visual baselines are synthetic, deterministic, complete, and manually reviewed.
- Comments/documentation comply with the active repository instruction.
- Decision Queue is empty or fully resolved.

Only `Gate 00: APPROVED` may authorize a phase commit and generation of IP-01.
