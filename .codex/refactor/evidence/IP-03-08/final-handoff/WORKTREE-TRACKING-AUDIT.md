# Uncommitted worktree tracking audit

Captured: 2026-08-21 (Asia/Shanghai)

## Scope

The audit covered the complete dirty worktree, including tracked diffs and
all paths returned by `git ls-files --others --exclude-standard`. Before this
adjustment the worktree contained 15 modified tracked paths and 294 visible
untracked files. The inherited Gate-01/02 dirty foundation was not cleaned,
reset, staged, committed, or deleted.

## Classification

### Keep visible for an intentional commit

- `src/`: 41 implementation files.
- `test/`: 28 files: 14 test/spec files and 14 baseline compatibility
  fixtures. The test source is part of the refactor's executable acceptance
  surface.
- `test-support/`: 10 test-support, smoke, lifecycle, and scope-verifier
  scripts.
- `.storybook/`, `playwright.config.ts`, `vitest.config.ts`, Vite configs, and
  TypeScript configs: 12 build/test configuration files.
- `.codex/refactor/`: 151 governance, baseline, migration, package, review,
  checkpoint, and final-handoff evidence paths. Required golden images and
  final manifests remain visible; only the previously identified redundant
  image captures are ignored.

No test source, fixture, test-support script, or test configuration was
classified as disposable. They must not be hidden through a broad `test/`,
`test-support/`, `*.test.*`, or fixture ignore rule.

### Do not track, but retain locally

- `storybook-static/`: 54 files generated by `npm run build:storybook`,
  including JavaScript bundles, fonts, workers, CSS, HTML, and SVG assets.
  It is reproducible output and has no source ownership in the repository.
- `test-results/.last-run.json`: 1 Playwright-generated run metadata file;
  it is not consumed by application code, tests, packaging, or migration.

These 55 files are now covered by the root `.gitignore`. They remain on disk
and can be inspected or regenerated; no deletion or overwrite was performed.

## Verification

The new generated-output rules are:

```text
storybook-static/
test-results/
```

Representative checks returned:

```text
git check-ignore -v --no-index storybook-static/app.js
  .gitignore:34:storybook-static/ storybook-static/app.js

git check-ignore -v --no-index test-results/.last-run.json
  .gitignore:35:test-results/ test-results/.last-run.json

git check-ignore -v --no-index test/refactor/ip-03-08/csv-worker-service.test.ts
  no match (visible)

git check-ignore -v --no-index test/fixtures/baseline/csv/manifest.json
  no match (visible)
```

The image-specific classification and counts are recorded in
`IMAGE-IGNORE-RESULT.md`. No Git stage or commit operation was performed.
