# SM-04 phase review

- Lifecycle state: `COMPLETE`
- Initial implementation commit: `b23a83efd9c0fbed611e37b8391e7b14b06b8337`
- Code-review correction commit: `69411f5a956ea1807715b32055cb74e6984e96c0`
- Formal clean-worktree review commit: `9b2a32da66eac33804fc0b208c535a869e1e669a`
- Toolchain: Xcode 26.3 (17C529), Swift 6.2.4, macOS 15.7.3 host, macOS 15.0 deployment target
- Formal Gate command: `./script/phase_gate.sh SM-04`
- Formal Gate result: `PASS`, `approvable=true`, exit `0`
- Formal Gate generated at: `2026-09-04T17:07:14Z`
- Local raw result: `/Users/rasteaks/Desktop/个人/GitHub/SlateSync-Swift/.codex/gate-results/SM-04/20260904T170557Z-9b2a32da66ea/`
- Formal result JSON: `/Users/rasteaks/Desktop/个人/GitHub/SlateSync-Swift/.codex/gate-results/SM-04/20260904T170557Z-9b2a32da66ea/result.json`

## Check summary

| Check group | Critical | Result | Evidence |
| --- | --- | --- | --- |
| Layout, tools, scope scan, diff integrity and Gate self-tests | Yes | PASS | Formal Gate passed; Gate helper fixtures 71/71 |
| SM-01 foundation and scope contracts | Yes | PASS | Five SwiftPM modules, Swift 6, macOS 15, historical baseline and generated-file boundaries verified |
| SwiftPM build and tests | Yes | PASS | `swift build`; `swift test` executed 96 tests with 0 failures |
| Xcode Debug and shared Test Plan | Yes | PASS | Debug build passed; Test Plan result bundle reported 3 passed, 0 failed, 0 skipped |
| SM-04 persistence contract | Yes | PASS | System SQLite v1 schema/pragmas, copied Library, snapshots, legacy migration, project isolation, transfer and tombstone deletion verified |
| Node compatibility baseline | Yes | PASS | `npm run test:node` 323/323 |
| Modern renderer baseline | Yes | PASS | `npm run test:modern` 25 files, 118/118 |
| Static checks and build | Yes | PASS | `npm run check`, `npm run typecheck`, `npm run build:modern` |
| Native ABI lifecycle | Yes | PASS | `npm run test:native:abi`; Electron and Node SQLite lifecycle checks passed |
| Approval freshness | Yes | PASS | Approval was recorded after this clean review commit; the governance commit is limited to this report and `CURRENT_STATE.json` |

## Scope and findings

- The native persistence layer preserves the frozen v1 filenames, complete Library/project schema and indexes, WAL, foreign keys, five-second busy timeout, private directory/file permissions and stable SQLite error mapping. `SQLiteDatabase.rows` returns only at `SQLITE_DONE` and fails closed on busy, locked or other step errors.
- The Library remains a project-folder index. Each project owns metadata/settings, tasks, diagnostics, Scenario Profiles and observations behind the same `ProjectRuntime` lease boundary. SQLite remains authoritative while task and diagnostic snapshots preserve unknown JSON fields and tolerate an independently malformed snapshot.
- Project and Library transfer rejects symbolic links, boundary aliases, future or malformed manifests and existing destinations. It stages ordinary files, uses SQLite online backup for every database, validates before atomic publication, omits transient sidecars and rebinds an imported project's Library, metadata, task, diagnostic and valid snapshot ownership without destroying malformed legacy evidence.
- Startup and activation persist and reopen arbitrary selected Library locations, migrate only known historical defaults, preserve the configured legacy path when the preferred destination conflicts, and return restart-required only after snapshot writers and all outgoing SQLite owners have been offered closure.
- Legacy global persistence migrates once into protected `project-default` from a read-only source using `legacy_migration_v1`. Destructive project deletion rejects new leases, drains active operations, closes all project stores, publishes a unique tombstone, compensates index deletion failure and retries safe physical cleanup at the next bootstrap.
- Code review found actor-reentrancy races in Library/startup/project-store bootstrap, project terminal-marker ownership, runtime close and suspended project acquisition; activation concurrency and rename ordering; concurrent Scenario import uniqueness; transfer enumeration error handling; failed SQLite backup cleanup; and incomplete outgoing-owner closure. Each finding was fixed and covered by a focused regression.
- Gate review also found that an xcresult UI automation initialization timeout could be misclassified and that a UI Test Runner built below this Desktop-hosted repository could trigger an interactive macOS Desktop-folder TCC prompt. Classification now preserves assertion/crash precedence, and Xcode Test Plan DerivedData, runner and live xcresult are created in the system temporary directory before the completed result is moved into Gate evidence. The formal clean Gate passed Unit/UI 3/3 without the prompt.
- Scenario learning and similarity remain explicitly deferred to SM-05. No schema v2, ORM, user-default Application Support access, real user Project Library access, Electron persistence replacement outside compatibility checks, or `.codex/refactor` historical-evidence rewrite entered SM-04.
- Blocking technical findings: none.

## Environment substitutions and residual risk

None. The formal Gate executed every required critical check directly for
`9b2a32da66eac33804fc0b208c535a869e1e669a`; it used no equivalent evidence,
`BLOCKED_ENV` replacement or Owner waiver.

Remote CI, Developer ID signing, notarization, release upload and SM-05 Scenario
learning remain later-phase or distribution concerns and are not SM-04 admission
requirements. All persistence tests used explicit temporary roots and did not
read, modify or delete the user's default Application Support or Project Library.

## Owner decision

- Decision: `APPROVED`
- Owner: `RaSteaks (Repository Owner)`
- Approved at: `2026-09-04T17:10:33Z`
- Review commit rechecked against HEAD: `yes`
- Non-critical exception expiry/revalidation: none

The implementation, independent code-review fixes, formal clean Gate and
Repository Owner approval are complete. SM-04 is admitted as `COMPLETE`; SM-05
may begin only as a separately authorized phase.
