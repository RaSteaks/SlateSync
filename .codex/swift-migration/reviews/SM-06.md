# SM-06 phase review

- Lifecycle: `IN_PROGRESS`; technical review is complete and Owner approval is pending.
- Formal clean-worktree review commit: `3ba200cafad758b10ad51c08eace5024bcffa90e`.
- Implementation base: `0429abf0980a02eb3db3834210e75344e5dc2b99`.
- Admission remains SM-05 COMPLETE; `CURRENT_STATE.json` is unchanged.
- Toolchain: Xcode 26.3 (17C529), Swift 6.2.4, macOS 15.7.3 (24G419), SDK 26.2.
- Host: MacBook Pro Mac15,8, Apple M3 Max, 16 logical processors, 64 GiB RAM.
- Target: macOS 15.0+, arm64 and x86_64; Debug runs on arm64.
- Formal Gate command: `SM06_PADDLE_RUNTIME_FILE=<isolated-runtime.json> ./script/phase_gate.sh SM-06`.
- Formal Gate result: `PASS`, 27 checks PASS, 1 approval check NOT_APPLICABLE,
  `approvable=true`, exit `0`; generated at `2026-09-05T08:17:41Z`.
- Formal result JSON: `/Users/rasteaks/Desktop/个人/GitHub/SlateSync-Swift/.codex/gate-results/SM-06/20260905T081330Z-3ba200cafad7/result.json`.
- The earlier dirty diagnostic PASS remains recorded at
  `.codex/gate-results/SM-06/20260905T074810Z-0429abf0980a/`; it is not the
  approval evidence. No Owner approval is claimed.

## Implemented boundary

Domain owns immutable media/evidence/configuration values, cancellation,
clock/deadline and transport protocols. Media owns ImageIO/PDFKit/CoreGraphics,
bounded Vision, the explicit Vision helper, Paddle process/protocol supervision,
selection, per-engine 8-item session-aware LRU and UTF-16 evidence formatting.
Workflow composes prepare/select/compress/OCR and an injected image-only
consumer, plus the unchanged Scenario v1 evidence adapter.

The 20 MiB/20-page limits, crop/detail geometry, JPEG dimensions and three
compression profiles, 0.94 whole-request UTF-8 budget, preset precedence,
LTRB conversion and evidence strings are retained. Framework objects remain
local to their owner. Canceled or obsolete work is drained before close,
cannot refill cleared caches and cannot invoke downstream recognition.

The shared `scripts/paddleocr_runner.py` is unchanged and copied as an Xcode
resource. No Provider transport/retry orchestration, task/CSV/SQLite schema,
SwiftUI workspace or distribution work enters this phase. The focused Workflow
facade is ready for SM-07 consumption; it is not yet wired into a recognition UI.

## Executed checks

| Check | Observed result |
| --- | --- |
| SwiftPM build and full tests | PASS; 139 cases, 138 passed, 1 dedicated Paddle-lane skip, zero failures |
| Dedicated offline Paddle lane | PASS; the skipped case ran separately, 1/1 passed, two actual inferences |
| Xcode Debug and shared Test Plan | PASS; 3/3 Unit/UI cases, zero failures/skips |
| SM-06 contract | PASS; 35 mapped acceptance IDs, fixture hashes and actual completed XCTest evidence |
| Gate self-tests | PASS; 78/78, including missing/mutated coverage and mixed failure/environment cases |
| SM-05 technical contract | PASS; existing CSV/metadata/Scenario contract and full Swift regression retained |
| Node compatibility | PASS; 323/323 |
| Modern renderer | PASS; 25 files, 118/118 |
| Static/type/build | PASS; `npm run check`, `npm run typecheck`, `npm run build:modern` |
| Native SQLite ABI | PASS; Electron and Node lifecycle probes |
| Real App launch | PASS; exact built executable, background launch, temporary Library creation and teardown |
| Debug/Release/Archive | PASS; Debug active architecture/-Onone, Universal Release and Archive, valid ad-hoc signatures |
| Earlier SM-02 platform contract | Additional PASS after preserving its entrypoint and artifact expressions |
| Scope/diff | PASS; five business modules, comments, protected history, no unsafe Sendable constructs |

The dedicated lane is mandatory for SM-06. Missing runtime/model evidence
remains BLOCKED_ENV and cannot pass through a fake or skipped test. Actual
assertion failures take precedence over environment text.

## Fixture and inference provenance

The manifest records 15 unchanged authority-source hashes, 21 media/oracle/fake
runner files and 2 Workflow fixture hashes, anchored at the base HEAD. Coverage
maps all MED/VIS/PAD/OCR/INT/RES/GOV IDs to executable lanes. Configuration has
83 independent vectors; selection has 192; Vision normalization has 4 vectors
extracted from the retained helper; evidence has 8 exact UTF-16 goldens.

| Fixture | Bytes | SHA-256 |
| --- | ---: | --- |
| `manifest.json` | 6644 | `72c15258e1a6f7f3314faad938c2eeff01f66ba09c925b2e99cb50be29fcf89e` |
| `coverage.json` | 6921 | `f800e3c9eb0c60d3c0552776926668f5922104f40d0f9c8ff8a637f2fa704959` |
| `geometry.json` | 11958 | `c4ec46d20d7abc6c683c9181dcb9dfedff46771bc7a3e757172cd1213406ab22` |
| `paddle-config.json` | 33479 | `4ded5e6ff80a75c5bbc8a79ac9c0e531f9ef575e298ca656d050904d38333081` |
| `selection.json` | 78534 | `13bd391cf3ca2269d5c58a977f86a326ee17842ba7efe0d542f70a9919e4f328` |
| `vision-oracle.json` | 10105 | `1a24e5b14f6f835533c5e5d13605ac6b7c0a95701bfae974492e969ecad6ca58` |
| `evidence.json` | 27046 | `303075a8326e638b7be57dae195cfa087e346c577ab90cc91741c4234d3dec31` |

Raster acceptance was frozen independently before native implementation:
exact dimensions/order, bounded allocations, white alpha and dominant-color
samples away from antialiased edges. JPEG-byte identity across frameworks is
not an acceptance criterion. Actual full/detail outputs from the final Gate
are retained at `/Users/rasteaks/Desktop/个人/GitHub/SlateSync-Swift/.codex/gate-results/SM-06/20260905T081330Z-3ba200cafad7/media-artifacts/`.
The review contact sheet is `media-artifacts/review.html`; representative full
PDF and repeated-header detail renders were visually inspected.

Actual Vision on this host used request revision 3 / macOS-Vision and returned
13 valid blocks from the synthetic fixture. Full text is intentionally not a
cross-OS golden. Actual Paddle used Python 3.9.13, PaddlePaddle
3.3.1, PaddleOCR 3.7.0, PaddleX 3.7.2,
custom PP-OCRv5/balanced with PP-OCRv5_mobile_det and PP-OCRv5_server_rec.
There was one warmup and two inferences, 31 blocks each, followed by zero
active/pending/worker state. The actual App bundle runner matched the source
byte-for-byte and ran with an unrelated cwd/cache.

The user explicitly supplied the sibling virtualenv and confirmed its sibling
model cache. Models were copied first; 38 copied files were hash-verified before
the real lane. Its HOME/cwd/TMPDIR/cache are temporary. The outer macOS sandbox
policy denies all networking, including native/Python libraries; SwiftPM's
`--disable-sandbox` only avoids an unsupported nested sandbox. No dependencies
or models were downloaded and the source runtime/cache were not modified.
The local runtime manifest used for reruns is recorded by
`/tmp/slatesync-sm06-runtime-path`; runtime versions are also retained in the
Gate artifact directory. Runtime manifests, model copies and generated outputs are not tracked.

## Resource and lifecycle evidence

RES-01 prepared 20 pages / 60 views and used actual protocol-only Python
processes for three consecutive prepare/OCR/close rounds. Formal Gate round
durations were 6.039998, 6.039181 and 6.028691 seconds. The test process
lifetime peak RSS was 755990528 bytes; initial RSS was 600965120; post-close
round snapshots were 523288576, 424722432 and 494813184 bytes. These
measurements include in-scope fixture values and the surrounding test
allocator, not only the media engine.

File descriptors returned from 4 to 4. After each close, pending=0, active=0
and managed processes=0; every recorded child PID had exited. Pipe ownership,
TERM/1000 ms/KILL/exit waiting, queued cancellation, shared recovery deadline,
configuration switches and generation invalidation have executable regressions.
There are no independent reader/timer tasks hidden behind a completion signal.
No post-hoc RSS/timing threshold or claim of zero allocator retention is used.

## Findings resolved during review and verification

The pre-commit review also found that Git treated frozen PDF fixtures as text,
so their required xref padding appeared as trailing-whitespace defects. A root
`.gitattributes` rule now classifies `*.pdf` as binary. No fixture bytes or
manifest hashes changed, and staged diff validation is clean.

The initial diagnostic Gate at `20260905T073641Z-0429abf0980a` exposed a UI
runner initialization timeout and a separate menu-bar-only restored App session.
It is retained as failed evidence, not edited or substituted.

The parsed xcresult correctly identified initialization as BLOCKED_ENV, but
Xcode's generic `** TEST FAILED **` banner incorrectly overrode that result.
The helper now preserves real assertion/case-failure precedence while allowing
the parsed summary to classify that generic banner. Regression coverage includes
the real banner and mixed assertion/environment failures. UI initialization
passed on the bounded rerun; no OS privacy settings or waivers were changed.

The real App was alive but its restored no-window state never constructed the
Project Library view/load task. Verification now uses the existing UI test's
`-ApplePersistenceIgnoreState YES` policy and an explicit LaunchServices test
root. The focused retry created the isolated Library after approximately 0.4 s;
the final full Gate passed the same background launch and cleanup checks.

## Remaining admission and handoff

No unresolved technical blocker remains. The dedicated review commit and its
exact-SHA clean Gate are complete with `approvable=true`; Owner approval has not
been requested or recorded. `CURRENT_STATE.json` therefore remains SM-05
COMPLETE, and SM-06 must not advance to COMPLETE or start SM-07 yet.

Actual v6 named-preset model inference was not exercised; those presets are
checked against frozen repository configuration oracles. Cross-OS Vision text,
production workload performance on other Macs and distribution remain outside
this evidence. The native Pipeline has no Provider or workspace UI integration.

SM-07 owns Provider transport/retry/concurrency and legacy TakeStatus string
conversion. SM-06 preserves source text and Scenario fingerprint
`e6ac0b81193ca50f1612d66ce5f1d586` and performs no learning writes. SM-08 owns
UI/installation and SM-09 owns distribution/Electron removal.
