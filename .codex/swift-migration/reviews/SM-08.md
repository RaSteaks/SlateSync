# SM-08 phase review

- Lifecycle state: `COMPLETE`; the clean Gate, evidence report, independent review, and
  Repository Owner approval are recorded.
- Exact review commit: `70fb5db50befaeeee3d44a38a6bf4e917542f2a7`.
- Product implementation commits: `d5faa78f5b3502d14346e61bf6b5a32ac440b147`,
  `70fb5db50befaeeee3d44a38a6bf4e917542f2a7`.
- Implementation base: `e3ed0ab6ff572720af70314e1a4b98db18d0b1fd`.
- Branch: `swift-rewrite`.
- Toolchain: Xcode 26.3 (17C529), Swift 6.2.4, macOS 15.7.3 (24G419),
  SDK 26.2, arm64 host.
- Formal Gate command: `./script/phase_gate.sh SM-08`.
- Formal Gate result: `PASS`, 28 checks PASS and one approval-freshness check
  `NOT_APPLICABLE` while the pre-approval state was still SM-07; `approvable=true`.
- Gate result directory:
  `.codex/gate-results/SM-08/20260906T104253Z-70fb5db50bef/`.
- Native evidence index:
  `.codex/gate-results/SM-08/20260906T104253Z-70fb5db50bef/native-evidence.json`
  with 45 acceptance-scoped JSON artifacts and a source fingerprint.
- Independent reviewer: agent `01a07660-ebe6-7722-97e1-866b148bac42`
  (Planck), read-only review of the final commit diff and SM-08 evidence scripts;
  no actionable P1, P2, or P3 findings. No tests were run and no files were modified
  by the reviewer.

## Check summary

| Check | Result | Evidence |
| --- | --- | --- |
| SwiftPM full regression | PASS | Gate `swift_test.log`; 226 tests, 1 dedicated Paddle skip, 0 failures |
| SM-08 ownership suite | PASS | 52/52 tests in `SM08OwnershipTests` |
| Native AppKit surfaces | PASS | 6/6 `SM08NativeSurfaceTests`, including accessibility and foreground cadence |
| Xcode shared Test Plan | PASS | 10/10 total: 2 product unit tests + 8 UI tests, 0 failures |
| UI routes and lifecycle | PASS | Launch, project create/open, Help, Logs, multi-window close/reopen, Settings close/return, Quit/relaunch |
| Chinese keyboard and persistence | PASS | Chinese project/task/prompt input, save, quit/relaunch, task-count restoration, prompt restoration |
| VoiceOver/accessibility semantics | PASS | Explicit Form labels/identifiers plus native CSV table/header/editable-cell assertions |
| Light/dark and minimum layout | PASS | 960 by 600 minimum frame, sidebar accessibility, 浅色/深色 screenshots and controls |
| Real SQLite scale | PASS | 500 projects + 1,000 tasks, 1 warm-up + 5 samples; project budget ≤1,500 ms, task budget ≤900 ms |
| Native list/CSV scale | PASS | 500/1,000 lazy native list evidence; 10,000-row CSV reuse/edit/memory/release evidence |
| Display-backed CSV cadence | PASS | Visible WindowServer-backed window, 10,000 rows, 119.69 FPS against ≥45 FPS budget |
| Compatibility matrix | PASS | Node 324/324; Modern 25 files and 118/118; static check/typecheck/build/native ABI |
| Build and packaging | PASS | SwiftPM Debug; Xcode Debug; universal Release and Archive; macOS 15.0; ad hoc strict codesign |
| Isolation and hygiene | PASS | Isolated test roots, clean review target, no tracked logs/`.xcresult`/archive/Gate artifacts |
| Governance contract | PASS | SM-08 source/fixture/coverage/evidence/AppKit allowlist/phase admission contract |

## Resolved implementation gaps

- The three UI failures were separated into product and locator causes. The
  `CommandGroup(replacing: .saveItem)` Close action had lost scene ownership;
  it now routes through `NSApp.keyWindow?.performClose(nil)`, so the window
  delegate can drain/approve the close while auxiliary Settings windows close
  normally.
- Multiline macOS Form fields and Pickers exposed their titles as sibling static
  elements. Workspace, project settings, project creation, and global appearance/
  density controls now expose explicit Chinese accessibility labels and stable IDs.
- Project creation and the Chinese reopen flow use focused keyboard commands and
  the explicit Open action, avoiding duplicated SwiftUI toolbar children while
  retaining end-to-end keyboard coverage.
- The native CSV surface now asserts table/header/cell semantics. A separately
  gated visible-window harness records real display cadence; ordinary SwiftPM
  runs keep that window test skipped.
- The formal Gate preserves raw Xcode XCTest PASS lines before generating
  acceptance evidence. The test progress probe uses Swift 6 `Mutex`; the
  window delegate's synchronous forwarding boundary uses balanced retained
  Objective-C ownership without `@unchecked Sendable`.

## Scope, compatibility, and risks

The original Electron/Node inputs and compatibility contracts remain intact.
The SM-08 implementation does not change the SQLite schema, CI/release cutover,
or signing policy. The local Archive proves universal bundle integrity,
minimum-system metadata, hardened runtime, and strict ad hoc codesign only; it
does not prove Developer ID notarization or distribution qualification, which
remain SM-09 concerns.

All UI, performance, lifecycle, screenshot, result-bundle, log, and generated
metric artifacts are outside the commit or under the ignored Gate result root.
The review target itself was clean before the Gate. The only tracked changes in
the approval commit are this review report and `CURRENT_STATE.json`.

## Owner decision

- Decision: `APPROVED`
- Owner: `RaSteaks (Repository Owner)`
- Approved at: `2026-09-06T11:07:39Z`
- Explicit approval basis: the Repository Owner authorized foreground final
  acceptance and explicitly instructed that SM-08 be completed with the goal
  `complete`, after which the exact clean review commit passed the formal Gate
  and the independent review reported no actionable findings.
- Approval is bound to the exact review commit
  `70fb5db50befaeeee3d44a38a6bf4e917542f2a7`; later product/source changes
  require a new Gate and approval.
