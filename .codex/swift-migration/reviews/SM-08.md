# SM-08 phase review

- Lifecycle state: `REVIEW_READY`; product code review is complete, but the phase is not approvable.
- Review commit: `dc00b6d15ec9119e308f5f38f0f38fbddb032e2a`.
- Implementation base: `0802248dbb75c110c063c37125db948b36fd52f9`.
- Branch: `swift-rewrite`.
- Toolchain: Xcode 26.3 (17C529), Swift 6.2.4, macOS 15.7.3 (24G419), SDK 26.2.
- Formal Gate command: `./script/phase_gate.sh SM-08`.
- Gate result: `BLOCKED_ENV`; the formal Gate was not run because it launches native UI tests, while the Owner required all tests in this run to remain in the background.
- Independent reviewer: agent `01a07569-6bba-7950-aa7c-d1b1226fef02` (Ramanujan), read-only review of the exact commit and its complete parent diff.
- Local evidence index: `.codex/swift-migration/reviews/SM-08-background-validation-20260906.md`.

## Check summary

| Check | Critical | Result | Evidence |
| --- | --- | --- | --- |
| Exact-commit Swift regression | Yes | PASS | `/private/tmp/slatesync-sm08-dc00b6d-full-swift.log`; 220 executed, 1 dedicated Paddle test skipped, 0 failures; SM08 owner suite 52/52 |
| Xcode Debug / Release / Analyze | Yes | PASS | `/private/tmp/slatesync-sm08-dc00b6d-{debug,release,analyze}.log` |
| Local universal Archive | Yes | PASS_LOCAL | `/private/tmp/slatesync-sm08-dc00b6d-archive.log`; `/private/tmp/slatesync-sm08-dc00b6d-signed.xcarchive` |
| Gate helper and SM08 contract negative tests | Yes | PASS | `/private/tmp/slatesync-sm08-dc00b6d-gate-selftest.log`; 82/82 helper checks and fail-closed SM08 self-test |
| Full SM08 contract | Yes | BLOCKED_ENV | `/private/tmp/slatesync-sm08-dc00b6d-contract-blocked.log`; correctly rejects absent native-surface execution before accepting a phase PASS |
| Retained Node/modern compatibility | Yes | PASS | Node 324/324, modern 118/118, static check, typecheck, production build and Node/Electron SQLite ABI lifecycle all pass; exact logs use the `/private/tmp/slatesync-sm08-dc00b6d-*` prefix |
| Native window, IME, keyboard, VoiceOver and visual checks | Yes | BLOCKED_ENV | Not executed under the background-only constraint; no equivalent-evidence substitution was accepted |
| Independent code review | Yes | PASS | No actionable P1, P2 or P3 code findings at the review commit |
| Owner decision | Yes | PENDING | No Owner approval for the exact review commit |

## Scope and resolved findings

The review commit closes the independent review's earlier code findings without
changing persistence schemas or retained Electron compatibility behavior:

- recognition admission remains closed until both the worker and service-side
  cancellation task drain; local import/generation also use operation identities;
- unavailable persisted Provider/model selections remain visible instead of being
  silently replaced, and an empty task cannot inherit a previous task's view state;
- Settings publishes a monotonic revision to Workspace and Project Settings, while
  recognition option loads reject late overlapping projections;
- Command-N is exposed only by the visible Workspace route with a current project;
- CSV selector routing covers Enter, Escape, Tab, Shift-Tab, arrows and Home/End,
  preserves marked text, and leaves native clipboard actions on the responder chain;
- the deterministic 10,000-row fixture contains Chinese, emoji, long and empty
  values, duplicate filenames, mixed statuses and mixed frame rates;
- broad interaction acceptance IDs remain in the manual/Gate lane, and native
  evidence now requires acceptance-scoped structured assertions and hashed artifacts.

The independent reviewer inspected all 16 changed files (637 additions and 129
deletions) relative to the implementation base, rechecked every earlier finding,
and reported **no actionable code findings** at P1, P2 or P3. The reviewer made no
file changes, ran no tests, and did not launch or display the application.

## Archive observations

The local Archive contains an `x86_64 arm64` universal executable, bundle identifier
`com.slatesync.app`, and minimum macOS version 15.0. `codesign --verify --deep
--strict` passes. Its CodeDirectory is `adhoc,runtime` and no Team ID is present;
therefore this proves local bundle integrity and hardened runtime only. It is not
Developer ID signing, notarization, distribution qualification or SM-09 cutover.

## Remaining critical evidence

The following acceptance work cannot be replaced by model tests or static review:

- Chinese IME candidate/composition/commit/cancel behavior in each real editor;
- keyboard-only navigation, Home/End, Tab/Shift-Tab and system clipboard routing;
- VoiceOver focus order, labels and announcements;
- Help navigation, multi-window close/reopen, Settings close/return, Command-N and
  full application termination/reopen behavior against the real key window/scene;
- light/dark appearance, contrast and the 960 by 600 minimum-window layout;
- complete native CSV scroll/edit/reuse/memory evidence and native lifecycle release.

The contract intentionally fails closed when native-surface execution or native
evidence is absent. The local JSON schema can validate structure, source freshness,
artifact hashes and per-acceptance scope, but cannot independently prove that its
claims came from a trusted human execution. Native evidence therefore remains an
Owner-attested trust boundary and cannot substitute for Owner approval.

## Environment substitutions

None. Historical XCUI output was used only to diagnose the removed Close command;
it was not treated as current PASS evidence. No equivalent-evidence file was used.

## Owner decision

- Decision: `PENDING`
- Owner: not recorded
- Approved at: not applicable
- Review commit rechecked against HEAD: `yes` when this report was prepared
- Non-critical exception expiry/revalidation: none

Approval is invalid while any critical native check is unexecuted, the formal Gate
is not PASS, or the Owner has not explicitly approved the exact review commit.
`CURRENT_STATE.json` must therefore remain at SM-07 `COMPLETE`; SM-09 is not admitted.
