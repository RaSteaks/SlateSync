# SM-09 implementation evidence and review handoff

## 双模型 review 修复（待新鲜 Gate）

首轮 clean Gate `cf268222995e7514c28cb422a5abab9247aeb828` 保留 FAIL，目录
`.codex/gate-results/SM-09/20260908T141957Z-cf268222995e`。Swift 237/1 skip/0 fail，
共享 Xcode 9 pass/2 fail，原生合同正确拒绝缺失执行；Archive/ZIP/DMG 通过。
打包 lane 记录 Desktop fixture 读取被拒绝以及 AX 授权问题。修复测试资源部署和
启动前台激活后，两个失败用例定向重跑 2 pass/0 fail/0 skip，结果在
`/private/tmp/sm09-review-focused-ui-first.xcresult`，bundle fixture 与源字节一致。
后续 clean Gate 必须覆盖该新增测试修复，不能沿用 cf26822 的失败记录作为 PASS。


Codex review 的失败分类复现成立，优先于 GLM 对原实现无技术阻断的判断。
六项删除映射、coverage 哈希约束和 fixture 来源均已修复并增加负例。
GLM 提供的摘录从 P3-3 开始，未包含 P2-1 的完整描述；本记录不假设缺失内容。
历史日志中的 Testing.framework Copy “exit code 0” 噪声不能单独证明测试失败或通过；
须结合 xcodebuild 状态与结构化 xcresult。失败计数无明细时也必须传递 FAIL 标记。

定向验证通过：Gate helper 103/103，release pipeline 16/16，native contract 7/7，
当前合同验证通过（239 删除项、49 fixture 的 HEAD/Git 原始哈希）。
完整 Gate 将在本次技术提交的干净 SHA 上执行；以下旧 Gate 仅作为历史证据。

仍阻断 COMPLETE：Dock/Spotlight CUA 再验超时；main/swift-rewrite 保护 API 均为
404 Branch not protected、有效 rules 数组为空，swift-rewrite 最近运行数组为空。
只读原始响应及重验记录见 `../manifests/sm09-review-external.json`。
需要完成桌面入口验收或合规 Owner 豁免、外部配置处置、修复后复审和最终批准。
本次未修改 CURRENT_STATE、远端设置或发布状态。


## 2026-09-08 收尾补验 / Closure validation

- Latest review commit: `01d8bce558f4adfb6a0da4e7c2c72462afa6c4b8`.
- Clean command: `./script/phase_gate.sh SM-09`; {'PASS': 26, 'NOT_APPLICABLE': 1}; approvable=true.
- Evidence directory: `.codex/gate-results/SM-09/20260908T044410Z-01d8bce558f4`; retained hash index `../manifests/sm09-closure-gate.json`.
- Xcode shared Test Plan: 11 passed; packaged Release UI: 9 passed.
- Added a real UI path opening the frozen legacy Library, importing/exporting CSV with
  canonical Shot/Take and CRLF checks, preserving the source, and recovering two tasks after quit/reopen.
- Bound UITargetAppPath and dependent products to the actual Release app while retaining the runner sandbox.
- Surfaced xcresult failure text so real assertions classify as FAIL even alongside environment logging.
- Fixed packaged cleanup by canonicalizing /var to /private/var and requiring process shutdown.
  Final packaged smoke reports `Packaged application process cleanup: PASS`.
- Isolated replacement of baseline 52b2a78 candidate with c9a4004 candidate kept all data hashes
  unchanged; CUA observed the saved prompt and original task in the replacement. Business sources
  under Sources/SlateSyncApp are unchanged between c9a4004 and this closure commit.
- CUA verified Finder double-click reopens a new window after closing the last window.
- Dock/Spotlight CUA surfaces timed out. These remain unverified, together with independent review
  and final Owner approval. The phase is **not COMPLETE** and no public distribution was performed.

新增端到端 UI、打包进程清理和隔离安装替换已有证据。下方是早先 c9a4004 的交接记录，
其中“替换旧应用”和“打包 CSV 路径待验证”已由本节补齐；Dock/Spotlight、独立审查及
最终批准仍待闭环。此前缓存清理是用户授权操作，旧哈希索引中的 DerivedData 条目已删除，
日志、xcresult、性能文件与 ZIP/DMG 仍保留。本次索引不包含可重建缓存。


- Status: technical Gate PASS; independent review and final Owner approval pending.
- Review commit: `c9a400435200484db049b03f1cd2f2b2b266bb4d`; branch `swift-rewrite`.
- Final command: `./script/phase_gate.sh SM-09` on a clean working tree.
- Result: `{'PASS': 26, 'NOT_APPLICABLE': 1}`, `approvable=true`.
- Evidence: `.codex/gate-results/SM-09/20260908T014931Z-c9a400435200`.
- Durable index: `../manifests/sm09-final-native.json` (logs, measurements and artifact hashes).
- Toolchain: Xcode 26.3 (17C529), Swift 6.2.4, SDK 26.2; macOS 15.7.3 arm64 host.
- This report records implementer verification, not an independent reviewer decision.

## Scope and verification / 范围与验证

239 historical production/build/test inputs were removed only after the clean compatibility
refresh on `52b2a78`. The immutable pre-cutover seal, Git ancestry, original source hashes,
49 fixture hashes, 235 retained test outcomes and 45 UI acceptance mappings are verified by
the native Gate. `.codex/refactor` retains its original Git tree. The final inventory contains
361 retained files and no legacy-remove or decision-required entries (the inventory itself
and later governance records are excluded from that implementation snapshot).

删除前完整兼容基线保持不变；删除映射可通过 Git 恢复。原生合同不伪造已删除 Node
测试的重新执行，而是核验封存结果并运行保留的原生测试。当前 CI/构建不依赖 Node。

- SwiftPM: `Executed 237 tests, with 1 test skipped and 0 failures (0 unexpected) in 72.357 (72.379) seconds`.
- Shared Xcode Test Plan: 10 passed; 0 failed.
- ZIP Release app XCUI: 8 passed; 0 failed;
  startup URL witnesses come from XCTest process stdout exported from `Packaged.xcresult`.
- Gate helper, release contract, packaging negative/cleanup tests and Release 10k CSV performance: PASS.
- Genuine old project/library exports: import, unknown-field retention, edit, reopen,
  export/reimport and unchanged project import source: PASS in isolated temporary roots.
- Universal archive and same-app ZIP/DMG: arm64+x86_64, macOS 15+, version 1.0.0/build 1,
  hardened runtime, ad-hoc strict signature, resource/dependency audit and checksum roundtrip: PASS.
- No push, tag, GitHub Release, production installation, real user Library modification or public signing.

## Evidence collection correction / 证据采集修复

The first dirty diagnostic reported eight passing packaged UI tests but failed its stdout
witness check because quiet xcodebuild does not emit parallel test runner output. The script
now exports xcresult diagnostics and requires all eight packaged URL witnesses. Its targeted
rerun passed, followed by this full clean Gate. The failed diagnostic was not rewritten as PASS.

首次诊断的界面测试通过，但日志见证缺失使 Gate 正确失败；修复保留了启动路径断言。
最终 Gate 使用全新结果目录，不将诊断结果当作正式批准依据。

## Remaining acceptance / 待验收

- Independent review is still required for deletion completeness, migrated assertions, ancestry,
  signing claims, permissions and data/rollback behavior. No independent reviewer is recorded here.
- Finder/Dock/Spotlight activation and replacing an actual previous-version bundle remain manual
  checks. The packaged UI suite covers eight existing scenarios; fake recognition/CSV export and
  old-package interoperability are verified in native suites, not one packaged end-to-end UI flow.
- The optional real Paddle inference lane requires an explicitly supplied offline runtime. Its
  environment-dependent skip is reported, not converted into a passing inference claim.
- Only local ad-hoc validation was authorized. Developer ID/notary/Gatekeeper public distribution
  and a downloaded public-release consumer smoke were not performed.
- `CURRENT_STATE.json` remains the approved SM-08 state. SM-09 must not become COMPLETE until
  independent review, remaining acceptance and explicit final Owner approval are recorded.

代码切换及自动 Gate 已完成；上述手工验收、独立审查与最终批准仍需闭环。本报告不宣称正式发布。
