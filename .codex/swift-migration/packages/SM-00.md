# SM-00 — Authority and baseline

Create the Swift migration authority set, freeze Electron compatibility inputs,
record the verified Xcode toolchain and isolate all test data. Gate: documents
agree, toolchain probes pass, and no historical evidence is rewritten.
