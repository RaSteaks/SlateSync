# SM-03 — Domain, settings, logs and credentials

Port typed contracts and validation, preserve configuration precedence and data
root, add structured OSLog, and migrate provider keys to Keychain transactionally.
Gate: fixture, redaction, fallback and migration tests pass.
