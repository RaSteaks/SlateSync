# SM-09 — Cutover and release

## 施工目标与文档状态

本施工包在 2026-09-05 细化，是 Swift 迁移的最终 cutover。目标是在仍可执行旧兼容矩阵时
完成最后一次差分，随后把构建、测试、归档、打包、CI 和 Release 全部切到原生 Xcode/Swift，
删除 Electron/React/Node production inputs，并验证一个 macOS 15.0+、arm64 + x86_64 的
Universal `SlateSync.app`、ZIP 与 DMG。数据升级、包导入导出、资源、签名和分发声明必须与
实际证据一致。

当前文档只生成施工计划，**SM-09 仍为 `NOT_STARTED`**。SM-08 尚未实施或批准；只有
`CURRENT_STATE.json` 将 SM-08 标记为 `COMPLETE`、`reviews/SM-08.md` 记录同一精确 SHA 的
clean Gate 和 Owner approval，且 Owner 另行授权 SM-09 后才可施工。本次不删除旧目录、
不改 CI/Release、不改签名、不生成发布包、不推进阶段状态，也不授权 commit/push/release。

## 准入、成功定义与不可变边界

### 准入检查

开始 WP-0 前必须同时满足：

1. SM-08 已按 `PHASE_GATES.md` 走完 `REVIEW_READY → PASS → COMPLETE`，状态文件与 review
   指向同一已批准 commit；dirty diagnostic 或旧阶段 PASS 不能替代。
2. 原生 App 已覆盖 Project Library、Workspace、Recognition、CSV、Settings、Logs、Help，
   所有 SM-08 acceptance/performance/resource tests 可从干净 checkout 执行。
3. Electron/React/Node 兼容源与测试仍完整存在，足以运行最后一次 pre-cutover matrix。
4. 工作树干净、release/cutover Owner 单独授权，删除清单和保留清单已审阅。

任何一项不满足，SM-09 保持 `NOT_STARTED`；不得用“先删后补测试”的方式制造不可验证状态。

### 成功定义

SM-09 的完成由两个连续、不可互相替代的证据点组成：

1. **Pre-cutover compatibility commit**：在旧 Node/Electron oracle 仍存在、且 WP-2～WP-5
   对 Xcode/resources/package/CI/release 的修改已经纳入后，完整 Swift + Electron/Modern/Node
   差分矩阵再次通过，冻结 source/output/evidence manifest；SM-08 approved commit 上的首次
   baseline 不能替代这个删除前最终证据点。
2. **Final native cutover commit**：其 parent/ancestry 可验证地包含上述 compatibility commit；
   Electron/React/Node production inputs 已删除，最终 Gate 只依赖 Swift/Xcode/macOS 系统工具，
   原生 Run/Debug/Test/Archive、bundle/package/data/signing audits 全部通过。

最终 Gate 不假装重新执行已经被合法删除的 Node 测试，而是校验 pre-cutover evidence 的精确
commit、hash、PASS 状态和 ancestry，并运行所有迁移后的原生 acceptance tests。旧测试不能在
没有等价覆盖/冻结证据时删除，也不能为了让 final Gate 变绿而静默降低断言。

### 不可变边界

1. 最终产品仅支持 macOS 15.0+，一个 Universal app 同时包含 arm64 与 x86_64；不得把两个
   单架构包命名成 Universal，也不恢复 Windows/Linux target。
2. 五个 Swift 业务模块、Project Library v1、SQLite schema、task/diagnostic/scenario JSON、
   Resolve CSV 字节、OCR-first、Provider/recognition 与 Keychain machine-local 语义不变。
3. `.codex/refactor/**` 是不可改写的历史证据，即使包含 `.mjs`、截图或旧路径也必须保留；
   它不进入 App bundle、CI runtime 或 production dependency，不能因“删除 Node”而清空。
4. Paddle Python runner 是原生产品的受管 OCR helper，不属于 Node/Electron。最终保留 Python
   源与 `requirements-ocr.txt`，但迁到唯一 canonical App resource 位置；不捆绑可执行 Python、
   venv、model cache、用户数据或联网安装产物。
5. 自动测试与包/data audit 使用临时 Application Support / Project Library、fake credentials/
   network/process；不得读取、迁移、删除或覆盖用户默认 Library 与 Keychain。
6. release secrets 只存在于受保护 CI secret/keychain/notary profile 边界；日志、artifact、
   xcresult、manifest、命令行回显和 Git 历史不得含 certificate、private key、password、API key。
7. 修改代码/脚本时更新签名、资源、工具版本、清理陷阱和数据升级等非显然注释，并同步根目录
   `AGENT.md`。不改写历史 review 或用事后编辑伪造既有 Gate evidence。

## 签名与分发声明分层

本阶段必须明确区分本地/PR 可执行验证与真正的外部分发，禁止把 ad-hoc 签名描述为已通过
Gatekeeper 的公开发布包。

| Lane | 必须证据 | 允许的声明 |
| --- | --- | --- |
| Local / PR archive | Release Universal app、hardened runtime、最小 entitlements、ad-hoc `-`、`codesign --verify --deep --strict` | “可构建、可归档、ad-hoc 签名的本地验证包” |
| Developer ID distribution | Developer ID Application 签名、secure timestamp、hardened runtime、完整 nested-code 验证 | “已用 Developer ID 签名” |
| Notarized public release | Developer ID + `notarytool` Accepted + app/DMG staple/validate + Gatekeeper assessment | “已公证并可供外部分发” |

Local/PR lane 是每次 Gate 必做且不需要私钥。若 Owner 要求正式外部分发，Developer ID/notary
lane 也是 release Gate 必做；本地缺 credential 时按 `PHASE_GATES.md` 记 `BLOCKED_ENV`，可由
受保护 CI 对同一 commit 的等价证据替代，但不能降级为 ad-hoc 后仍声称 notarized。若本阶段
只获授权做 cutover/ad-hoc archive，review 必须明确“未发布、未验证 Gatekeeper 公证”，不得
创建 GitHub Release。

## 当前实现、最终目标与权威来源

| 当前实现 | 当前状态 | SM-09 目标 |
| --- | --- | --- |
| `SlateSync.xcodeproj` | macOS 15、Debug/Release、Release hardened runtime、手工 ad-hoc、版本 1.0.0/1 | 冻结 Universal/signing/version/resource 设置，增加可验证 distribution lane |
| shared scheme/test plan | Run/Test/Profile/Archive 已存在 | clean checkout 中成为唯一 App 构建/测试入口 |
| `script/build_and_run.sh` | 原生 build/run/debug/verify 已可用 | 保持开发入口并补 release-safe artifact/version checks |
| `script/phase_gate.sh` | 仍要求 node/npm 并执行 `.mjs` contracts/旧矩阵 | pre-cutover 全矩阵后改为 final native-only Gate 与迁移后的 contract tests |
| `.github/workflows/ci.yml` | setup-node、npm ci、Node/Modern/ABI + 原生 Gate | 只安装/选择 Xcode，运行 Swift/Xcode/native Gate，上传必要 evidence |
| `.github/workflows/release.yml` | 双 arch Electron builder，上传 `dist/*.dmg`/`*.zip` | 单一 Universal native archive，Developer ID/notary lane，ZIP/DMG/checksum/release |
| `electron-builder.yml` / package files | Electron 当前生产打包入口 | compatibility freeze 后删除 |
| `electron/ lib/ public/ src/` | Electron main/renderer/legacy runtime | 等价原生覆盖确认后删除 |
| `test/ test-support/` | Node/Modern/Electron contracts、fixtures、performance | evidence 冻结并迁移必要 fixtures/assertions 后删除旧 executable suites |
| `scripts/paddleocr_runner.py` | Xcode 从旧 scripts 路径复制 | 移入原生 Resources/PaddleOCR，代码/Xcode/tests 使用同一 canonical source |
| `.codex/refactor/` | 旧产品历史证据 | 原样保留、明确排除于 bundle/runtime |

WP-0 必须读取并 hash：`MASTER_PLAN.md`、`MIGRATION_MATRIX.md`、`COMPATIBILITY_CONTRACT.md`、
SM-00～SM-08 packages/reviews、`CURRENT_STATE.json`、Xcode project/scheme/test plan、Info.plist、
entitlements、build/gate scripts、两份 workflow、Electron production/runtime/test inventory、
所有迁移 fixture 与 Paddle resources。

## 产物与版本契约

1. 默认保留当前 macOS 发布形式：同一个已验证 Universal `SlateSync.app` 生成 `.zip` 与 `.dmg`。
   如果 Owner 决定只保留一种格式，必须在 WP-0 记录兼容/发布决策，不能在实现中顺手删除。
2. Release 文件名包含产品版本且不声称单架构，例如 `SlateSync-<version>-macOS-universal.zip`
   与 `.dmg`。二者必须来自同一 app payload/hash lineage；生成 `SHA256SUMS` 与 JSON manifest。
3. tag `vMAJOR.MINOR.PATCH`、`CFBundleShortVersionString`/`MARKETING_VERSION` 和 artifact version
   必须精确一致。`CFBundleVersion`/`CURRENT_PROJECT_VERSION` 使用 WP-0 冻结的单调整数来源；
   无合法 tag 或版本不一致时 release fail-closed。
4. DMG/ZIP 构建命令、volume layout、压缩方式、locale/timezone 和输入清单应可重现；由于 DMG、
   code signature/notary timestamp 可能非逐字节确定，不要求跨运行 hash 相同，但必须保证内容、
   app hash lineage、架构、版本、签名与 resource manifest 可追溯。
5. `.xcarchive`、DerivedData、临时 keychain、export staging、DMG mount、notary submissions 与最终
   artifacts 默认写入忽略的 Gate/result 临时目录；除有意的 GitHub Release artifact 外不提交 Git。

## 施工分包

### WP-0 — 最终 inventory、删除分类、版本/发布决策与验收图谱

1. 生成 `sm09-inventory.json`：每个 tracked 文件标记 `native-product`、`native-test`、
   `release-input`、`migration-history`、`legacy-remove`、`fixture-migrate` 或 `decision-required`，
   并记录 path、size、SHA-256、引用者和最终 owner。
2. 建立删除前引用图：Xcode resources、Package.swift、shell scripts、workflows、docs、fixtures、
   Gate contracts 与 runtime paths。`rg` 命中只是候选，删除前必须确认动态路径/资源查找。
3. 冻结 pre-cutover compatibility matrix、运行顺序、工具版本、成功计数、evidence schema、
   commit/ancestry 校验与 retention。没有结果文件/SHA 的命令不能只凭终端摘要算 PASS。
4. 冻结 version mapping、ZIP/DMG 保留决策、Developer ID/notary 是否为本次必需 lane、release
   notes/source archive policy、最低 Xcode 版本和 CI runner image。
5. 生成 `sm09-coverage.json`，把每个旧 Node/Electron test family 映射到 Swift unit/integration/
   UI/perf test、冻结 fixture 或历史-only evidence。任何 `legacy-remove` 项无 owner 时停止。
6. 冻结最终 bundle allowlist/denylist、entitlements baseline、dynamic library allowlist、Paddle
   resource manifest、data-upgrade fixture manifest 和验收 ID 映射。

交付：inventory、reference graph、removal/retention/decision lists、coverage、release contract。

### WP-1 — 完整兼容矩阵与删除前证据封存

1. 先从干净的 SM-08 approved commit 运行 baseline：所有 SwiftPM/Xcode Unit/UI、SM-01～08
   technical contracts、
   Debug/Release/Archive、隔离 App launch、Electron Node tests、Modern tests/typecheck/build、Electron
   smoke/ABI 与已冻结差分 fixtures。
2. 重新执行 Library v1/SQLite/task/diagnostic/scenario、project/library package、CSV encodings/bytes、
   metadata、media/PDF/ImageIO/Vision/Paddle fake/offline、Provider prompt/schema/normalization、
   recognition cancellation/concurrency 和完整原生 UI workflow matrix。
3. 使用复制的 v1 Library/项目包做 native open/edit/close/reopen/export/reimport，不碰用户目录；
   检查 unknown JSON fields、WAL/backup、archive/tombstone、Keychain reference 和日志权限。
4. 运行 500-project、1,000-task、10k CSV、multi-window、terminate/drain 和资源释放套件；记录
   当前机器/Xcode/Swift/macOS 与预冻阈值，不因进入 release 阶段跳过性能。
5. 输出 immutable `pre-cutover-result.json`、命令日志 hashes、fixture/source manifest、coverage、
   xcresult/performance indexes 和 commit SHA。原始 evidence 放忽略目录，review 提交摘要/manifest。
6. 任一旧/新差分失败先在旧输入仍存在时修复并重跑；不得先删除失败 oracle。baseline
   PASS 后可实施 WP-2～WP-5，但在 WP-6 删除前必须把这些变更组成/指向一个干净的
   pre-cutover commit，再完整重跑本 WP；只有刷新后的 matrix 全 PASS、evidence 指向该
   删除前 commit 且 review 确认 coverage 完整，才可进入 WP-6。

交付：可由 hash/commit 重验的 pre-cutover PASS evidence；它成为后续删除的硬前置。

### WP-2 — Xcode Release、资源、版本、entitlements 与架构收敛

1. 审核 project/scheme/test plan：Release `MACOSX_DEPLOYMENT_TARGET=15.0`、
   `ARCHS_STANDARD`/arm64+x86_64、`ONLY_ACTIVE_ARCH=NO`、Swift 6、hardened runtime；Debug
   保持开发签名/可调试，不能把 Release 安全设置复制错方向。
2. Info.plist 的 bundle ID、display/name、short/build version、minimum OS、document/URL types
   与实际产品一致。版本从显式 release input 注入，禁止脚本默默编辑 tracked project 后打包。
3. `SlateSync.entitlements` 采用实际能力需要的最小集合；空 entitlements 可继续为空。新增
   sandbox、network、file、Apple Events、Keychain group 或 disable-library-validation 必须先有
   功能/签名依据与回归，不能为“让签名通过”临时放宽。
4. 将 `scripts/paddleocr_runner.py` 与 `requirements-ocr.txt` 移入
   `SlateSyncApp/Resources/PaddleOCR/` canonical tree；更新 Xcode、SM-06 runtime/tests/manifests。
   bundle 内只留 source/requirements，不含 venv、cache、model、`.pyc`、secret 或用户文件。
5. 审核 icon、Help、Paddle 和其他 resources 只有一个来源，大小/hash 在 manifest 中；删除
   renderer HTML/CSS/JS、Electron Framework、asar/pdfjs 等资源引用。
6. 分别产生 Debug、ad-hoc Release 与（有 credential 时）Developer ID archive，验证
   `file`/`lipo`、Info.plist、entitlements、hardened runtime、nested code 与 codesign chain。

交付：Xcode release configuration、canonical resources、version/signing tests 全部通过。

### WP-3 — 原生 archive/export/package pipeline 与 bundle audit

1. 建立单一 `script/archive_release.sh`（名称可收敛），非交互调用 shared scheme
   `xcodebuild clean archive`，显式 DerivedData/archive/result paths、configuration、destination、
   version 与 signing lane；任何未解析变量、空路径或输出落入仓库 tracked tree 都失败。
2. local lane 使用 ad-hoc Release 验证；distribution lane 让 Xcode/明确 export options 处理
   Developer ID identity/team，不在脚本中拼接私钥。每个命令记录已脱敏 argv、工具版本和 status。
3. archive artifact audit：app/executable 存在，arm64+x86_64，minimum OS 15.0，Info version/tag
   一致，hardened runtime/entitlements/signature 符合 lane，所有 nested executable 都被签名。
4. dependency audit 使用 `otool -L`/`file`/`lipo`/bundle traversal：只允许系统 frameworks 和
   本项目批准内容；拒绝 Electron/Chromium/Node dylib、Electron Framework、`app.asar`、
   `node_modules`、`.js/.mjs/.cjs/.ts/.tsx/.map`、renderer HTML/CSS、package files、venv/cache。
   允许 manifest 中的 `.py`/requirements Paddle resources。
5. 从同一 audited app staging 生成 ZIP 与 DMG；验证安全路径、无 symlink escape、无额外
   顶层 payload、DMG 可只读挂载/卸载、内部 app hash/version/signature 与源 lineage 一致。
6. 生成 `SHA256SUMS` 和 artifact manifest：commit/tag/version/build、toolchain、lane、app/bundle
   manifest hash、ZIP/DMG size/hash、sign/notary fields。失败或中断通过 trap 清理 mount/staging。
7. package pipeline self-tests 使用 fake tool outputs/临时目录覆盖空 version、单架构、错误资源、
   symlink、签名失败、mount 失败、partial output、并发 release 与 cleanup；不需要证书即可运行。

交付：可重放的 native archive/package 命令、Universal ZIP/DMG 与严格 bundle audit。

### WP-4 — CI 切换为 Swift/Xcode-only

1. 将 `.github/workflows/ci.yml` 改为固定 macOS runner/Xcode 版本，验证 `xcodebuild -version`、
   `swift --version`，运行 Gate self-tests、SwiftPM、Xcode shared test plan、ad-hoc Release/Archive、
   isolated launch 和 bundle audit。
2. 删除 `actions/setup-node`、npm cache、`npm ci`、npm/node/npx/TypeScript/Vite/Vitest/Playwright/
   Electron steps。最终 CI required tools 清单不含 Node；Python 只用于 Paddle helper 的静态/fake
   测试或显式离线 lane，不能联网安装依赖。
3. CI 所有 App/data tests 显式设置临时 root 和忽略窗口恢复，headless/TCC 环境失败按现有
   fail-closed classification；不能因 UI runner 不稳定而删除 Xcode UI suite。
4. cache 只覆盖安全可复用的 Swift/Xcode dependency/build cache，key 含 lock/project/toolchain；
   不缓存 Keychain、DerivedData 中带绝对用户路径的 release products、Paddle venv/model 或数据。
5. 上传失败诊断与必要 xcresult/manifest，设定 retention；不上传项目 fixture 副本、用户路径、
   secret 或完整 Provider payload。PR CI 永不导入 distribution certificate/notary credential。
6. required check 名称、branch protection 和 README badge/命令同步；旧 npm check 不再被保护规则
   永久等待。workflow 权限最小化，普通 CI 只需 contents read。

交付：从 clean checkout 仅靠 Xcode/Swift/macOS 系统工具通过的 CI。

### WP-5 — Release workflow、Developer ID、公证与 artifacts

1. `.github/workflows/release.yml` 只允许经过验证的 `vMAJOR.MINOR.PATCH` tag 或显式受保护
   dispatch；先运行同 commit native Gate，再构建一次 Universal archive，禁止 arm64/x64 matrix
   各自产生不同 app 后合并或同名上传。
2. 在临时 keychain 导入最小 Developer ID certificate，设置 partition list 后立即缩小搜索范围；
   password/private key 只从 protected secrets 输入。脚本使用 trap 删除 keychain/staging，任何
   `set -x`、security dump 或 secret argv/log 都禁止。
3. 用 Developer ID/hardened runtime 签名并执行 `codesign --verify --deep --strict`、签名详情和
   `spctl` 评估。缺/过期/错误 team/certificate 明确失败或 `BLOCKED_ENV`，不回退 ad-hoc。
4. 公证策略在 WP-0 固定并自动化：`xcrun notarytool submit --wait`，验证 Accepted；staple/validate
   app，再由该 app 生成 final ZIP/DMG；DMG 按策略另行 submit、staple、validate。最终从 DMG
   挂载验证包含的 app、Gatekeeper、version/arch/signature。ZIP 内 app 必须带已 staple ticket。
5. release artifacts 包括 ZIP、DMG、`SHA256SUMS`、JSON manifest 和中英 release notes；不上传
   `.xcarchive`、private symbols、notary credential、temporary keychain 或未审计中间包。若需要
   dSYM，作为受限独立 artifact，并在 policy 中明确。
6. 创建 GitHub Release 是外部写操作，必须在用户明确授权后执行；draft/prerelease 标志、tag、
   artifact hashes 与 notes 在发布前再次核对。失败 release 不覆盖既有 tag/artifact。
7. 下载已发布 artifacts 做独立 consumer smoke：checksum、挂载/解压、app path、launch、临时
   Library create/open、quit/reopen、Gatekeeper/signature。不得仅验证 workflow 上传成功。

交付：有授权与 credential 时的 Developer ID + notarized + stapled ZIP/DMG；否则只交付明确
标注的 ad-hoc evidence，不声称正式发布完成。

### WP-6 — 删除 Electron/React/Node production inputs 并迁移 Gate

1. 依据 WP-0 inventory 和 WP-1 PASS 删除 tracked legacy runtime/build：`.storybook/`、
   `electron/`、`lib/`、`public/`、`src/`、Electron-only `scripts/*.mjs`/shell/Swift helper、
   `electron-builder.yml`、`package.json`、`package-lock.json`、TypeScript/Vite/Vitest/Playwright
   configs 和 Electron signing entitlements。
2. 删除旧 executable test suites：`test/`、`test-support/` 中已经由 Swift/native fixtures 覆盖
   的 Node/Modern/Electron tests/helpers。仍需的 CSV/JSON/media/Python fixtures 先复制/迁移到
   对应 `Tests/*/Fixtures/` canonical path、更新 manifest/hash/引用，再删除旧目录。
3. 保留 `.codex/refactor/**` 原样作为历史；根目录 `UX-CONTRACT.md`、`DESIGN.md`、migration
   packages/reviews/manifest 继续作为文档/审查输入，但不进入 product bundle。
4. `script/tests/sm02...sm08*.mjs` 与 `phase_gate_tests.zsh` 中的 Node inline tests 必须迁为 Swift
   unit tests或 zsh/macOS tool assertions。最终 `phase_gate.sh SM-09` 与 self-tests 不执行或要求
   `node`、`npm`、`npx`，也不要求已删除路径存在。
5. 早期 source manifests 中的 legacy path/hash 作为 provenance 保留；新增 final cutover manifest
   记录 removed path、pre-cutover hash、removal reason、replacement acceptance IDs。不要改写旧
   manifest 让它看似从未引用过 Electron。
6. 清理 `.gitignore`、README、AGENT、build docs、IDE config、workflow、licenses/attributions 与
   shell paths。删除 Node ignores/dependencies 的同时保留 Swift/Xcode/Gate result/Paddle runtime
   需要的安全 ignores。
7. 全仓 `rg` 审计残留：产品/CI/release/script 不得引用 Electron、React、Node、npm、Vite、
   Playwright、electron-builder、旧 renderer/resource path。历史文档/manifest 命中必须落入明确
   allowlist，不能用全局 ignore 掩盖活引用。
8. 删除后立即运行 final native Gate；若等价测试缺失、resource path 断裂、data fixture 丢失或
   early gate 被静默弱化，恢复/补齐当前工作树内容后再继续，不改写 pre-cutover evidence。

交付：native-only tracked production/CI/release tree、final cutover manifest 与 migrated Gate。

### WP-7 — 数据、包、升级、权限与用户目录审计

1. 在删除后用 WP-1 冻结的复制 fixture 重跑 v1 Library：首次启动、已有 active/archived 项目、
   task/diagnostic/scenario、unknown fields、settings Library path、关闭/reopen 与并发 lease。
2. 用 pre-cutover Electron 导出的 `.slatesync-project` / `.slatesync-library` 在 final native app
   import，编辑、识别 fake result、CSV export、关闭后重开，再 export 并由 native validator
   reimport。源包保持只读，所有操作在临时根。
3. 验证默认路径/旧全局数据一次性迁移只复制/记录，不删除原源；自定义 Library 原址语义、
   restart-required、目录权限、WAL/backup、tombstone cleanup 与失败恢复不因 cutover 改变。
4. 验证 global settings v2/custom Provider/revision/model capabilities 与 Keychain reference；
   secrets 不进入 package/manifest/log。旧 Electron key store 若有显式迁移路径，必须使用测试
   Keychain/fake，生产迁移幂等且不回显。
5. 执行升级安装 smoke：旧版本 app/data 存在、替换 app bundle、首次启动、project open、
   settings/log/help、Paddle runtime check、quit/reopen。自动化使用独立临时目录，不替换真实
   `/Applications/SlateSync.app` 或用户数据。
6. 审计 package traversal/symlink/hardlink、恶意 manifest、权限拒绝、磁盘满/只读目标、partial
   copy、crash/restart。失败保持原数据与源包，不以 destructive repair 自动“修好”。
7. 记录支持/不支持的升级来源和 rollback：App bundle 可替换回旧版本，但数据库一旦未来升版
   的策略需单独决定；本阶段 schema 仍 v1，因此不得无意写入不可回退格式。

交付：final native data/package/upgrade matrix，无用户目录副作用和格式漂移。

### WP-8 — 文档、启动体验、运维与 release smoke 收口

1. README 改为原生唯一入口：macOS 15+、Xcode/Swift 要求、`build_and_run.sh`、Xcode Run/Test/
   Archive、settings/key、Paddle 可选安装、data location/backup、logs/help 与已知限制；删除 npm/
   Electron/Windows/Linux 当前操作说明。
2. `AGENT.md` 记录最终 native architecture、build/test/release、legacy removal、Paddle resource、
   signing/notary lane、data contract 和当前治理状态；历史章节明确为历史，不造成当前误导。
3. 验证 Finder/Dock/Spotlight 首次启动、无窗口 activate/reopen、Settings、文件面板、主菜单、
   app icon/name/version、Help/Logs、crash-free quit；检查控制台无缺 resource/signing warning。
4. release/support runbook 写明 checksum、Gatekeeper、日志、Library backup/import/export、Paddle
   检查、常见签名/公证失败、撤回/替换 release；不建议用户绕过 Gatekeeper 或关闭系统安全。
5. license/third-party notices 与实际 final bundle/dependencies 对齐；删除 Electron/React/Node 后
   移除不再分发的 notice，但保留仍分发的 SQLite/system/Python package requirements 说明。
6. 对最终 ZIP/DMG 运行全新用户 smoke（临时 home/data root）：launch、create project/task、
   fake/local workflow、CSV export、Settings/Help/Logs、quit/reopen；无真实 Provider/安装网络。

交付：native-only 用户/开发/release 文档与 packaged-app smoke evidence。

### WP-9 — 最终 Gate、独立审查、发布授权与治理完成

1. `./script/phase_gate.sh SM-09` 在 final tree 只要求 git/rg/python3（仅 Paddle 静态检查需要时）/
   swift/xcodebuild/xcrun/lipo/file/otool/codesign/spctl/plutil/ditto/hdiutil 等 macOS 工具；不得要求
   node/npm/npx 或 deleted sources。
2. Gate 验证 pre-cutover evidence commit/hash/PASS/ancestry、final coverage/removal manifests、
   source tree denylist、SwiftPM/Xcode、Run/Debug/Test/Profile/Archive、Universal/minimum OS、bundle/
   resources/dependencies、ad-hoc signing、ZIP/DMG、data/package/upgrade、docs 和 repository hygiene。
3. distribution lane 另验证 Developer ID team/timestamp/hardened runtime、notary Accepted、staple、
   `spctl` 与 consumer artifact smoke；无 credential 时依据已冻结 scope 记 BLOCKED_ENV 或校验同
   commit 的 CI equivalent evidence，不得用假数据通过。
4. 施工期 dirty Gate 始终 `approvable=false`。用户另行授权后创建 dedicated final review commit；
   在精确 SHA 干净工作树运行 Gate，原始结果放忽略目录，review 写 `reviews/SM-09.md`。
5. 独立审查覆盖删除 completeness、测试迁移、evidence ancestry、签名/公证真实性、secret cleanup、
   release workflow permissions、bundle/data audits、升级/回退和文档。任何未决 P0/P1 阻断完成。
6. 创建/上传 GitHub Release 需要再次获得用户明确授权；SM-09 技术 PASS 本身不授权外部发布。
   已授权发布时，review 记录 tag、release URL、artifact/checksum/notary evidence，不记录 secret。
7. 只有 final clean Gate PASS、review 无阻断、必要 distribution/equivalent evidence 满足且 Owner
   明确批准后，治理提交才把 SM-09 置为 `COMPLETE`。这是迁移完成，不代表未来所有 release
   永久有效；后续版本仍必须运行 release Gate。

交付：final native cutover clean Gate、review、可选已授权 release evidence 与 Owner approval。

## 删除与保留清单

### 计划删除（以 WP-0 inventory 为最终准绳）

```text
.storybook/
electron/
lib/
public/
src/
test/                         # 必要 fixtures/assertions 先迁移
test-support/                 # 历史截图/摘要若需长期保留则进 .codex/refactor
electron-builder.yml
package.json
package-lock.json
playwright.config.ts
tsconfig*.json
vite*.config.ts
vitest.config.ts
scripts/build-vision-ocr.mjs
scripts/copy-pdfjs.mjs
scripts/electron-build-host.mjs
scripts/electron-dev.mjs
scripts/setup-paddleocr.sh     # 若原生 installer 已完全替代
scripts/vision_ocr.swift       # 若 SM-06 native Vision 已完全替代
```

`script/tests/*.mjs` 不是简单删除：其仍有治理价值的断言先迁入 Swift/zsh native contracts，再
删除源文件。任何实际仍被 native bundle/test 引用的资源在迁移前都不得删除。

### 必须保留或迁移后保留

```text
.codex/refactor/**                         # immutable migration history
.codex/swift-migration/**                  # packages/reviews/contracts/manifests
Sources/**, Tests/**                       # native product and tests
SlateSyncApp/**, SlateSync.xcodeproj/**
Package.swift, SlateSync.xctestplan
script/build_and_run.sh
script/phase_gate.sh, script/lib/**
.github/workflows/ci.yml
.github/workflows/release.yml
UX-CONTRACT.md, DESIGN.md, README.md, AGENT.md
SlateSyncApp/Resources/PaddleOCR/
  paddleocr_runner.py
  requirements-ocr.txt
```

`.env.example`、`slatesync.config.json`、legacy entitlements、old icons 和根目录其他配置由 WP-0
逐项判定；不得仅凭扩展名批量删除。

## 施工顺序与停止点

```text
SM-08 COMPLETE + 单独授权
  → WP-0 inventory/coverage/release decisions
  → WP-1 baseline compatibility（旧源仍在）
  → WP-2 Xcode/resources/version/signing
  → WP-3 archive/package/bundle audit
  → WP-4 native-only CI
  → WP-5 protected release/notary lane
  → WP-1 refresh：删除前精确 commit 的 full compatibility PASS
  → WP-6 legacy removal + Gate migration
  → WP-7 data/package/upgrade audit
  → WP-8 docs + packaged smoke
  → WP-9 diagnostic → review commit → clean Gate → review → Owner/release authorization
```

停止条件：

- WP-1 matrix 未全 PASS 或 evidence 无精确 SHA/hash，不得删除任何 legacy oracle。
- 任一删除项没有 replacement acceptance ID、历史用途或明确 Owner 决策，不得删除。
- Universal、minimum OS、hardened runtime、entitlements、bundle denylist 或 data audit 失败，不得打包。
- Developer ID/notary credential 缺失时停止正式分发 lane，不得降级 ad-hoc 后继续发布。
- release workflow 可能回显 secret、遗留临时 keychain/mount 或拥有过宽权限时不得启用。
- final Gate 仍依赖 node/npm/npx、deleted path 或用户默认 Library 时 cutover 失败。
- 未经明确外部发布授权，不创建 tag、GitHub Release、上传 artifact 或替换用户安装。

## 计划文件落点

```text
SlateSyncApp/Resources/PaddleOCR/
  paddleocr_runner.py
  requirements-ocr.txt

script/
  build_and_run.sh
  archive_release.sh
  package_release.sh
  verify_bundle.sh
  verify_distribution.sh
  phase_gate.sh
  lib/phase_gate_lib.sh
  tests/
    phase_gate_tests.zsh
    sm09_contract_tests.zsh

Tests/SlateSync*Tests/Fixtures/SM09/
  data-upgrade/
  package-transfer/
  malicious-packages/
  sm09-manifest.json
  sm09-coverage.json

.codex/swift-migration/reviews/SM-09.md
.codex/swift-migration/manifests/
  sm09-inventory.json
  sm09-pre-cutover.json
  sm09-cutover.json

.github/workflows/ci.yml
.github/workflows/release.yml
```

如 manifest 目录在现有迁移结构中采用其他名称，可在 WP-0 统一，但 pre-cutover 与 final
cutover 记录必须是 tracked、可 hash、可审查的长期证据；大体积日志/xcresult/artifact 继续忽略。

## 验收 ID

### Pre-cutover compatibility matrix

| ID | 验收条件 |
| --- | --- |
| MAT-01 | SM-08 approved clean commit 与 toolchain/source manifest 精确冻结 |
| MAT-02 | SwiftPM/Xcode Unit/UI/Run/Debug/Release/Archive 全通过 |
| MAT-03 | Node/Electron/Modern/type/build/smoke/ABI 在删除前全通过 |
| MAT-04 | Library/SQLite/task/diagnostic/scenario/package 差分全通过 |
| MAT-05 | CSV/metadata/media/OCR/Provider/recognition 差分全通过 |
| MAT-06 | 500 project/1k task/10k grid/lifecycle/resource 达标 |
| MAT-07 | pre-cutover result 含 commit/hash/log/coverage 且可重验 |
| MAT-08 | final commit ancestry 可验证地包含该 compatibility point |

### Archive、bundle 与架构

| ID | 验收条件 |
| --- | --- |
| ARC-01 | clean archive 只使用 shared Xcode scheme/native sources |
| ARC-02 | app executable 同时含 arm64+x86_64，最低 macOS 15.0 |
| ARC-03 | tag/marketing/build/Info/artifact version 一致且 build 单调 |
| ARC-04 | Release hardened runtime 与最小 entitlements 精确符合 lane |
| ARC-05 | Help/Paddle/icon 等资源唯一、hash 正确、无缺失 |
| ARC-06 | bundle 无 Electron/Chromium/Node/asar/JS/TS/map/node_modules |
| ARC-07 | dylib/framework allowlist、nested executable 与 codesign 验证通过 |
| ARC-08 | ZIP/DMG 来自同一 audited app，manifest/checksum/mount smoke 通过 |

### Signing、notarization 与 release

| ID | 验收条件 |
| --- | --- |
| SIG-01 | Local/PR ad-hoc archive deep/strict 验证通过且不夸大分发声明 |
| SIG-02 | Developer ID lane 的 identity/team/timestamp/runtime 验证通过 |
| SIG-03 | notarytool 返回 Accepted，app/DMG staple 与 validate 通过 |
| SIG-04 | spctl 与挂载后内部 app 的签名/架构/version 通过 |
| SIG-05 | certificate/notary/temp keychain 全程不泄漏且中断后清理 |
| SIG-06 | 缺环境按 BLOCKED_ENV/equivalent evidence 处理，不伪 PASS |
| REL-01 | release 只接受合法 tag/受保护 dispatch，先过同 commit Gate |
| REL-02 | 一次 Universal build，不以双 arch matrix 冒充同一 app |
| REL-03 | ZIP/DMG/SHA256SUMS/manifest/release notes 内容一致 |
| REL-04 | workflow permissions/cache/artifact retention 最小且安全 |
| REL-05 | PR CI 不读取 distribution secrets，release lane 才临时导入 |
| REL-06 | 发布后下载/checksum/mount/launch/data smoke 通过 |
| REL-07 | failed release 不覆盖旧 tag/artifacts，cleanup 完整 |
| REL-08 | 外部 tag/Release/upload 只有用户明确授权后执行 |

### Cutover、CI 与 repository

| ID | 验收条件 |
| --- | --- |
| CUT-01 | inventory 中每个删除项有 pre-hash、reason 与 replacement ID |
| CUT-02 | Electron/React/Node runtime/build/package/config 已从当前树删除 |
| CUT-03 | executable Node/Modern/Electron tests 在等价迁移后删除 |
| CUT-04 | `.codex/refactor/**` 原样保留且不进入 runtime/bundle |
| CUT-05 | Paddle runner/requirements 迁到唯一 native resource 且测试通过 |
| CUT-06 | final CI/Gate/release 不要求 node/npm/npx 或 deleted paths |
| CUT-07 | 全仓残留命中仅在历史 allowlist，无 live legacy references |
| CUT-08 | README/AGENT/licenses/ignores/commands 全部 native-only 且一致 |

### Data 与治理

| ID | 验收条件 |
| --- | --- |
| DAT-01 | copied v1 Library 在 final app open/edit/close/reopen 无漂移 |
| DAT-02 | legacy project/library packages import/export/reimport 兼容 |
| DAT-03 | settings/custom Provider/Keychain reference 升级幂等且不泄漏 |
| DAT-04 | custom/default Library、migration、permissions、tombstone 安全 |
| DAT-05 | malicious/partial/read-only/disk failure 保持源数据与可恢复性 |
| DAT-06 | packaged upgrade/quit/reopen smoke 使用临时根且不碰用户数据 |
| GOV-01 | pre-cutover + final clean Gate + review + Owner 后才标记 COMPLETE |

## 完成定义与最终交接

SM-09 只有在 pre-cutover 全矩阵证据完整、legacy removal 有逐项替代、最终 tree/CI/Gate/release
不依赖 Node、Universal archive 和 ZIP/DMG 的架构/资源/签名/data audits 通过、独立 review 无
阻断项并获得 Owner 最终批准后才完成。如果 scope 声称公开分发，还必须有同 commit 的
Developer ID/notary/staple/Gatekeeper evidence；只有 ad-hoc 时必须明确限制声明。

主要风险包括：在最后一次差分前删除 oracle、把历史 manifest 改写成“从未存在”、最终 Gate
仍暗含 Node、双单架构包冒充 Universal、ad-hoc 冒充 notarized、release secret/临时 keychain
泄漏、DMG/ZIP 不是同一 app、Paddle helper 被误删或捆绑 venv，以及自动升级测试碰到真实用户
Library。所有风险都由两点证据链、严格 inventory/denylist、临时根、签名分层和 fail-closed
Gate 关闭。

SM-09 `COMPLETE` 表示当前迁移和首次 native release pipeline 已获批准；后续每个版本仍需按
同一 native Gate、版本、签名、公证、artifact 和数据 smoke 流程执行，不能复用旧 notarization
结果或把本阶段审批当作永久发布授权。
