# 开发/CI 可继续使用此模板；桌面用户优先在“全局设置”中填写普通配置与 API Key。
# .env 只作为启动时的回退来源，不要把真实密钥提交到 Git。

# OpenAI 官方 API：支持 gpt-5.6-luna、gpt-5.6-terra、gpt-4o-mini
OPENAI_API_KEY=
OPENAI_BASE_URL=https://api.openai.com/v1

# OpenRouter API：支持本项目内全部四个固定模型
OPENROUTER_API_KEY=
OPENROUTER_BASE_URL=https://openrouter.ai/api/v1

# 可选：随 OpenRouter 请求发送的应用标识
OPENROUTER_SITE_URL=https://github.com/RaSteaks/SlateSync

# 阿里云 Token Plan：仅在已获得应用后端调用许可时启用
TOKENPLAN_API_KEY=

# 个人版通常使用下列兼容地址；团队版请替换为控制台显示的专属 Base URL
TOKENPLAN_BASE_URL=https://dashscope.aliyuncs.com/compatible-mode/v1

# 阿里云百炼（DashScope）：在百炼控制台创建 API-KEY；兼容 OpenAI 接口
# 支持 qwen-vl-max-latest、qwen3-vl-plus-latest、qwen-vl-plus-latest 等视觉模型
DASHSCOPE_API_KEY=
DASHSCOPE_BASE_URL=https://dashscope.aliyuncs.com/compatible-mode/v1

# 旧版单槽位兼容环境变量（继续支持）。新安装建议在全局设置中添加多个自定义接口；
# 新接口配置写入 userData/global-config.json，密钥独立保存在 provider-keys.json。
# 自定义接口 API Key 可留空，留空时 Main 不发送 Authorization。
OPENAI_COMPATIBLE_API_KEY=
OPENAI_COMPATIBLE_BASE_URL=https://your-provider.example/v1
OPENAI_COMPATIBLE_MODEL=your-vision-model

# chat-completions（默认）或 responses
OPENAI_COMPATIBLE_API_MODE=chat-completions

# chat-completions 可选：json_schema、json_object（默认）或 prompt
OPENAI_COMPATIBLE_JSON_MODE=json_object

# 可选：auto、low、high（默认）或 original
OPENAI_COMPATIBLE_IMAGE_DETAIL=high

# 可选：SlateSync 工作流配置文件；相对路径以项目根目录为基准
SLATESYNC_CONFIG_PATH=slatesync.config.json

# Electron 运行参数；通常保持默认值
MAX_BODY_MB=80
# 单个模型请求超时（毫秒）：高精度多模态识别大图可能较慢，默认 180000，范围 30000–3600000
MODEL_REQUEST_TIMEOUT_MS=180000
# 单个模型请求超时后的自动重试次数，默认 1，范围 0–3
MODEL_REQUEST_MAX_RETRIES=1
# 同时提交给模型的页数，默认 2；服务商限流时可降为 1，吞吐充足时可升至 3–6
MODEL_PAGE_CONCURRENCY=2
MAX_CONCURRENT_RECOGNITIONS=1

# PaddleOCR 证据层：auto（检测到 .venv-paddleocr 时启用）、true 或 false
PADDLEOCR_ENABLED=auto
PADDLEOCR_REQUIRED=false
PADDLEOCR_MODEL_VERSION=PP-OCRv6
# custom 保留下方手动参数；命名预设会同时切换 PP-OCRv6 模型、批量和输出过滤
PADDLEOCR_PRESET=custom
# custom 下兼容 PP-OCRv5 的 fast、balanced（默认）或 accurate
PADDLEOCR_PROFILE=balanced
PADDLEOCR_LANGUAGE=ch
PADDLEOCR_DEVICE=cpu

# macOS Vision OCR 证据层：auto（检测到 Swift 工具链时启用）、true 或 false；
# 在 macOS 上比 PaddleOCR 更轻量，识别与坐标提取完全在本地完成
VISIONOCR_ENABLED=auto
VISIONOCR_REQUIRED=false
# 识别语言；可写逗号分隔的列表，如 zh-Hans,en-US；留空时自动检测
VISIONOCR_LANGUAGE=zh-Hans
# accurate（默认，高精度）或 fast（快速）
VISIONOCR_RECOGNITION_LEVEL=accurate
# 是否启用语言校正（自动纠错）
VISIONOCR_USE_LANGUAGE_CORRECTION=true
VISIONOCR_MIN_CONFIDENCE=0.10
# 0 表示保留全部文本块；大于 0 时均匀覆盖整页，不会只截掉底部
VISIONOCR_MAX_BLOCKS_PER_VIEW=0
# auto 按页面视图数量扩展；也可填写明确的毫秒数，最大 30 分钟
VISIONOCR_TIMEOUT_MS=auto
# 可选：自定义编译后的二进制路径；默认自动用 swiftc 构建到 bin/vision-ocr
VISIONOCR_BINARY=

# 可选：覆盖性能档中的模型与识别批量；一般无需修改
PADDLEOCR_DETECTION_MODEL=
PADDLEOCR_RECOGNITION_MODEL=
PADDLEOCR_RECOGNITION_BATCH_SIZE=

# 可选：自定义 Python；默认使用项目内 .venv-paddleocr/bin/python
PADDLEOCR_PYTHON=
PADDLEOCR_MIN_CONFIDENCE=0.10

# 0 表示保留全部文本块；大于 0 时会均匀覆盖整页，不会只截掉底部
PADDLEOCR_MAX_BLOCKS_PER_VIEW=0
# 检测最长边（320–4096）；custom 留空使用 PaddleOCR 默认值
PADDLEOCR_TEXT_DET_LIMIT_SIDE_LEN=

# auto 按页面视图数量扩展；也可填写明确的毫秒数，最大 60 分钟
PADDLEOCR_TIMEOUT_MS=auto

# 可选：模型权重缓存位置；默认使用项目内、不会提交 Git 的 .paddlex-cache
PADDLE_PDX_CACHE_HOME=
