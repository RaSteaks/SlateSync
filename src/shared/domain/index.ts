/** Domain names re-export the Shared Contract DTOs; no second shape source is introduced. */
export type { ConfigData, JsonSchemaCapabilityResult, LibraryInfo, ModelData, OcrCheckResult, OcrSelection, OcrSettings, ProgressData, ProjectData, RecognitionData, ScenarioData, TaskData, VisionOcrCheckResult } from "../contracts/index.js";
