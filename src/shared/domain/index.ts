/** Domain names re-export the Shared Contract DTOs; no second shape source is introduced. */
export type { ConfigData, LibraryInfo, ModelData, OcrCheckResult, OcrSettings, ProgressData, ProjectData, RecognitionData, ScenarioData, TaskData } from "../contracts/index.js";
